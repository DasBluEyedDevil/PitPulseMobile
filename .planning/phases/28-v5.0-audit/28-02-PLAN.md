---
phase: 28-v5.0-audit
plan: 02
type: audit
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-v5.0-audit/28-02-FINDINGS.md
files_forbidden:
  - backend/src/**
  - backend/migrations/**
  - mobile/lib/**
sequential_files: []
expected_artifacts:
  - .planning/phases/28-v5.0-audit/28-02-FINDINGS.md
autonomous: false
agents:
  - qa-verification-specialist
  - senior-developer
requirements:
  - AUDIT-01
  - D1
  - D2
  - D3
  - D4
  - D5
verification_commands:
  - "test -f .planning/phases/28-v5.0-audit/28-02-FINDINGS.md && echo 'exists'"
  - "grep -c '^### D[1-5]' .planning/phases/28-v5.0-audit/28-02-FINDINGS.md"

must_haves:
  truths:
    - "FINDINGS.md covers all 5 D-flows (D1-D5) with per-flow h3 sections (### D1 ... ### D5)"
    - "Every gap claim backed by file path + line reference OR METHOD /path endpoint"
    - "Each gap classified per CONTEXT §2 evidence rubric (BROKEN requires reproduction)"
    - "Each gap tagged with verification mode: STATIC-PASS / RUNTIME-VERIFIED / BLOCKED-OPS-02"
    - "Each gap assigned severity per CONTEXT §4 concrete rubric and proposed fix sketch"
    - "Satori templates rendered with real user data (RUNTIME-VERIFIED) and checked for overflow + WCAG contrast"
    - "Landing page deep links (iOS + Android) verified against real bundle IDs / package names; iOS gap tagged BLOCKED-OPS-02 if Apple Developer still pending"
    - "Pro entitlement enumeration: every Pro-gated code path touched by v5.0 flows inventoried, source-of-truth (`users.is_premium`) cross-verified per CONTEXT Standing Assumptions"
    - "QA Verification Specialist has final sign-off on every gap entry; partner agent contributes traces"
  artifacts:
    - ".planning/phases/28-v5.0-audit/28-02-FINDINGS.md"
  key_links: []
---

<objective>
Audit the Social/Sharing E2E pipeline (flows D1-D5) and produce a findings document at `.planning/phases/28-v5.0-audit/28-02-FINDINGS.md` enumerating every gap blocking end-to-end sharing functionality. This is a **read-only audit phase** — do not modify any backend, template, or mobile source files.

Every claim must cite `file:line` or a concrete API endpoint. D-flows span three layers: mobile share triggers, backend satori card pipeline, and the web landing page — trace all three.

The consolidator (Plan 28-03) merges this output with 28-01-FINDINGS.md into `.planning/milestones/v5.0-AUDIT.md`.
</objective>

<context>
@.planning/phases/28-v5.0-audit/28-CONTEXT.md
@.planning/exploration-v5.0-feature-completion.md
@.planning/milestones/v2.0-AUDIT.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Audit D1 (Celebration share) + D2 (Badge unlock share)</name>
  <files>.planning/phases/28-v5.0-audit/28-02-FINDINGS.md</files>
  <action>
**D1 — Check-in celebration share** (success = post-checkin celebration -> IG Stories / X / TikTok share card with app-store CTA):

1. Mobile entry: trace `mobile/lib/src/features/checkin/presentation/celebration_screen.dart`. Record the share button(s) and the share card preview widget. v2.0-AUDIT BETA-26 confirmed `ShareCardPreview` is elevated above badges at `celebration_screen.dart:166` — confirm current state.
2. Share card fetch: trace the HTTP call to `GET /api/share-cards/checkin/:id` (or equivalent). Record endpoint and response shape.
3. Backend generation: trace `ShareCardService.generateCheckinCard()` -> satori template `backend/src/templates/checkin-card.ts`. Record file:line.
4. Platform-specific share: how does mobile dispatch to IG Stories vs. X vs. TikTok? Are there 3 separate share actions or a single system share sheet? Record the share SDK / package (e.g., `share_plus`, custom platform channel).
5. App-store CTA: confirm the rendered card includes a "Get the app" CTA or a URL that resolves to the landing page. Record where this CTA text lives in the template.
6. Image export: does mobile save the card as PNG/JPG locally before handing to the share sheet? Record the image cache path.

**D2 — Badge unlock share** (success = badge detail -> shared card -> landing page):

1. Mobile entry: trace `mobile/lib/src/features/badges/presentation/badge_detail_screen.dart`. Record the share action on badge detail.
2. Share card fetch: trace `GET /api/share-cards/badge/:id` -> `ShareCardService.generateBadgeCard()` -> `backend/src/templates/badge-card.ts`.
3. Unlock trigger: when a badge unlocks (via `BadgeService`), is the user prompted to share, or is sharing only initiated from badge detail? Trace `BadgeService.checkAndAwardBadges()` to see any share-prompt side effect.
4. Landing page link: verify the shared badge card includes a URL pointing at the landing page (D4 flow), and that the URL encodes the badge ID for landing-page rendering.

Write D1 and D2 sections to `.planning/phases/28-v5.0-audit/28-02-FINDINGS.md` using this structure:

```markdown
### D1: Check-in celebration share

**As-is state**: <short trace summary>

**Gaps identified**:

| ID | Gap | Classification | Severity | Evidence | Proposed Fix |
|----|-----|----------------|----------|----------|--------------|
| D1-G1 | <description> | BROKEN/MISSING/PARTIAL/UNTESTED/DEGRADED | HIGH/MED/LOW | `file:line` or `METHOD /path` | <1-2 sentence sketch + est. LOC> |
```

Do not modify source. Read-only.

> verification: grep -c '^### D[12]' .planning/phases/28-v5.0-audit/28-02-FINDINGS.md
  </action>
  <verify>
grep -c '^### D[12]' .planning/phases/28-v5.0-audit/28-02-FINDINGS.md
  </verify>
  <done>D1 and D2 sections in FINDINGS.md with per-gap evidence. Share-sheet platform integration path documented.</done>
</task>

<task type="manual">
  <name>Task 2: Audit D3 (Wrapped recap share) + D5 (Satori card rendering)</name>
  <files>.planning/phases/28-v5.0-audit/28-02-FINDINGS.md</files>
  <action>
**D3 — Wrapped annual recap share** (success = Pro feature; story slides + branded recap cards):

1. Mobile entry: trace `mobile/lib/src/features/wrapped/presentation/wrapped_*_screen.dart`. Record the Wrapped story-slide UI and the share action.
2. Premium gate: verify `requirePremium()` middleware or equivalent on `GET /api/wrapped`. v2.0 BETA-16 confirmed block filter on Wrapped — re-verify. Record file:line.
3. Share card fetch: trace `GET /api/share-cards/wrapped/*` -> `ShareCardService.generateWrapped*Card()` -> `backend/src/templates/wrapped-*.ts` templates. Enumerate all Wrapped template variants (story slides and share cards).
4. Story slide export: how does mobile export the animated story slides? Native video render, PNG sequence, or just a static card?
5. Pro gating on mobile: verify the Wrapped entry point hides or paywalls for non-Pro users.
6. **Pro entitlement cross-cutting inventory** (cross-cutting per CONTEXT §Cross-Cutting Surfaces): enumerate ALL Pro-gated v5.0 code paths beyond D3 (e.g., are any D1/D2 share variants Pro-only? premium badge tiers? extended Wrapped depth?). Cross-verify the source-of-truth is `users.is_premium` written by RevenueCat webhook (per CONTEXT Standing Assumptions). Flag any divergent entitlement check (e.g., reading from RevenueCat SDK directly on mobile, or a `subscriptions` table query) as PRECONDITION-VIOLATION.

**D5 — Satori card rendering** (success = templates render with real user data, no overflow, WCAG-accessible):

1. Template inventory: list every satori template under `backend/src/templates/*.ts`. Confirm file count and naming.
2. Test rendering: locate any test file or script that renders templates against fixture data (e.g., `backend/tests/templates/*.test.ts`). **RUNTIME-VERIFIED requirement**: render at least one template per type (badge / checkin / wrapped) against fixture data — either via existing test, ad-hoc node script invoking `ShareCardService`, or hitting prod `https://soundcheck-app.up.railway.app/api/share-cards/*` with curl + saving the PNG. Inspect output. If no rendering possible, flag as UNTESTED — HIGH severity.
3. Overflow handling: for each template, inspect the layout — are long usernames, band names, venue names truncated (with ellipsis) or do they overflow? Record the layout strategy (flex wrap, line-clamp, max-width).
4. WCAG contrast: for each template's color palette, check foreground/background ratios against WCAG AA (4.5:1 for text). v3.0-AUDIT confirmed mobile contrast; share cards are separate. Record color refs and ratios (use approximations from hex values).
5. Emoji / special char rendering: satori has known gotchas with emoji fonts. Verify font loading in `ShareCardService.ts` or template setup.
6. Landing-page.html consistency: the landing page (`backend/src/templates/landing-page.html`) should visually match the share cards. Flag style drift.

Append D3 and D5 sections to `28-02-FINDINGS.md`. (D4 handled in Task 3.)

Read-only.

> verification: grep -c '^### D[35]' .planning/phases/28-v5.0-audit/28-02-FINDINGS.md
  </action>
  <verify>
grep -c '^### D[35]' .planning/phases/28-v5.0-audit/28-02-FINDINGS.md
  </verify>
  <done>D3 and D5 sections in FINDINGS.md. Complete template inventory + overflow + WCAG findings per template.</done>
</task>

<task type="manual">
  <name>Task 3: Audit D4 (Shared link landing page + deep links)</name>
  <files>.planning/phases/28-v5.0-audit/28-02-FINDINGS.md</files>
  <action>
**D4 — Shared link landing page** (success = non-user click -> web landing w/ correct App Store + Play Store deep links):

1. Landing page: open `backend/src/templates/landing-page.html`. Record its current structure, styling, and the route that serves it.
2. Serve route: trace `backend/src/routes/` for the HTTP route (e.g., `GET /share/:id`, `GET /c/:id`, or similar). Record the controller and parameter shape.
3. Dynamic content: does the landing page render the shared check-in / badge / Wrapped card image inline? Or does it pull it fresh from ShareCardService?
4. Deep links:
   - **iOS App Store**: record the App Store URL or Universal Link. If Apple Developer enrollment is still pending (per OPS-02), the App Store URL will be a placeholder — flag as MISSING with HIGH severity blocked on OPS-02.
   - **Android Play Store**: record the Play Store URL using the real package name (`com.soundcheck.app` or actual — verify from `mobile/android/app/build.gradle.kts`).
5. Domain: current landing lives at `soundcheck-app.up.railway.app` per exploration notes. Verify the configured base URL in the backend. Flag if `getsoundcheck.app` custom domain is referenced but not actually bound.
6. Meta tags / OG: social previews (Twitter Card, OG image) — does the landing page emit the correct meta tags so the shared link looks rich on IG / X / FB?
7. No-user flow: what does a non-authenticated visitor see? (Read-only preview of the card? Prompt to install? Both?)
8. Bot/crawler handling: does the landing page work for social media crawlers that render OG images?

Append D4 section to `28-02-FINDINGS.md`.

Then add a closing **D-Flow Summary** section:

```markdown
## D-Flow Summary

| Flow | Gaps Found | Highest Severity |
|------|------------|------------------|
| D1 | N | HIGH/MED/LOW |
| D2 | N | HIGH/MED/LOW |
| D3 | N | HIGH/MED/LOW |
| D4 | N | HIGH/MED/LOW |
| D5 | N | HIGH/MED/LOW |
| **Total** | N | — |
```

Read-only audit. Do not modify.

> verification: grep -c '^### D4' .planning/phases/28-v5.0-audit/28-02-FINDINGS.md
  </action>
  <verify>
grep -c '^### D4' .planning/phases/28-v5.0-audit/28-02-FINDINGS.md
  </verify>
  <done>D4 section and D-flow summary counts in FINDINGS.md. All 5 D-flow sections present. App Store + Play Store deep link state documented (including OPS-02-blocked iOS state).</done>
</task>

</tasks>

<verification>
- [ ] `.planning/phases/28-v5.0-audit/28-02-FINDINGS.md` exists
- [ ] Contains 5 sections `### D1` through `### D5`
- [ ] Every gap entry includes file:line or endpoint evidence
- [ ] Every gap classified (BROKEN/MISSING/PARTIAL/UNTESTED/DEGRADED) with severity
- [ ] Summary table at end counts gaps per flow
- [ ] Satori template inventory complete (every file in `backend/src/templates/*.ts`)
- [ ] Deep links for iOS + Android documented (placeholder state flagged if applicable)
- [ ] No backend or mobile source files modified
</verification>

<success_criteria>
- FINDINGS.md is a trustworthy input to Plan 28-03 consolidation
- Every gap claim is falsifiable against cited file:line
- OPS-02 carry-forward (Apple Developer pending) correctly noted where it blocks verification
- WCAG contrast and overflow findings for every satori template
</success_criteria>

<output>
After completion, `.planning/phases/28-v5.0-audit/28-02-FINDINGS.md` is ready for Plan 28-03 consolidation. Do NOT create a SUMMARY.md — consolidation plan handles that.

**Do NOT modify ROADMAP.md or STATE.md from this plan** — Plan 28-03 is the sole writer to those files (avoids Wave 1 race with Plan 28-01). This plan only writes its FINDINGS.md.
</output>
