---
phase: 28-v5.0-audit
plan: 03
type: consolidate
wave: 2
depends_on:
  - 28-01
  - 28-02
files_modified:
  - .planning/milestones/v5.0-AUDIT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
files_forbidden:
  - backend/src/**
  - backend/migrations/**
  - mobile/lib/**
sequential_files:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
expected_artifacts:
  - .planning/milestones/v5.0-AUDIT.md
autonomous: false
agents:
  - senior-project-manager
  - reality-checker
requirements:
  - AUDIT-01
verification_commands:
  - "test -f .planning/milestones/v5.0-AUDIT.md && echo 'exists'"
  - "grep -c '^### B[1-5]\\|^### D[1-5]' .planning/milestones/v5.0-AUDIT.md"
  - "grep -c '^### Phase [0-9]' .planning/milestones/v5.0-AUDIT.md"

must_haves:
  truths:
    - ".planning/milestones/v5.0-AUDIT.md exists and covers all 10 flows (B1-B5, D1-D5)"
    - "Every gap entry from 28-01-FINDINGS and 28-02-FINDINGS is merged into the audit doc with classification + severity + verification mode + evidence + proposed fix"
    - "Cross-flow integration gaps (shared file appears in 2+ flow gap evidence) extracted into dedicated section BEFORE per-flow merge — prevents duplicate fix work in Phase 29+"
    - "Design-resolution step: every MISSING/PARTIAL gap has explicit desired-behavior statement, not just 'fix it' — provides Phase 29+ a spec, not a defect list"
    - "Gaps grouped into candidate Phase 29+ phases (count derived from gap density, NOT anchored to arbitrary 6-8 target). If total gaps >40, halt for user scope decision."
    - ".planning/REQUIREMENTS.md placeholder B1-B5 and D1-D5 entries replaced with concrete requirements derived from gap findings + design-resolution"
    - ".planning/ROADMAP.md v5.0 section updated with Phase 29+ entries (goal, depends_on, requirements, success criteria, plan count estimate)"
    - ".planning/STATE.md reflects Phase 28 complete and points to next phase"
    - "Reality Checker sampling protocol: open N=10 random gap entries, verify cited file:line behaves as claimed, append confirms/contradicts table to 28-03-SUMMARY.md. Fail consolidation if any sample contradicts."
    - "PRECONDITION-VIOLATION findings (if any auditor flagged Standing Assumption breakage) routed to a separate severity-HIGH section, not absorbed into B/D flow gaps"
  artifacts:
    - ".planning/milestones/v5.0-AUDIT.md"
  key_links:
    - ".planning/phases/28-v5.0-audit/28-01-FINDINGS.md"
    - ".planning/phases/28-v5.0-audit/28-02-FINDINGS.md"
---

<objective>
Consolidate the two audit findings documents (28-01-FINDINGS.md + 28-02-FINDINGS.md) into a single authoritative audit at `.planning/milestones/v5.0-AUDIT.md`, then concretize v5.0 requirements and seed Phase 29+ entries in ROADMAP.md. This closes AUDIT-01 and transitions v5.0 from audit to execution.

Reality Checker verifies: every claim in the final AUDIT doc cites a file:line or endpoint from a FINDINGS file. Nothing fantasy, nothing unverified.

Follow the v2.0-AUDIT.md format: requirements coverage matrix, cross-phase integration table, gap inventory, grouped remediation plan.
</objective>

<context>
@.planning/phases/28-v5.0-audit/28-CONTEXT.md
@.planning/phases/28-v5.0-audit/28-01-FINDINGS.md
@.planning/phases/28-v5.0-audit/28-02-FINDINGS.md
@.planning/milestones/v2.0-AUDIT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Pre-flight integrity check + cross-flow extraction + merge findings into v5.0-AUDIT.md</name>
  <files>.planning/milestones/v5.0-AUDIT.md</files>
  <action>
**Step 1.0 — Pre-flight integrity check** (FAIL LOUD if either FINDINGS file is malformed):
- `grep -c '^### B[1-5]' .planning/phases/28-v5.0-audit/28-01-FINDINGS.md` MUST return `5`
- `grep -c '^### D[1-5]' .planning/phases/28-v5.0-audit/28-02-FINDINGS.md` MUST return `5`
- If either count off: stop, surface to user with the actual counts and missing flow IDs. Do NOT silently merge incomplete findings.

**Step 1.1 — Cross-flow extraction (BEFORE per-flow merge)**:
1. Parse all gap entries from both FINDINGS files. Extract the Evidence column (file paths and endpoints).
2. Build a map: `file_path -> [list of gap IDs that cite it]`.
3. Any file appearing in **2 or more** gap entries across different flows is a **cross-flow integration candidate**.
4. Open dedicated section `## Cross-Flow Integration Gaps` in the merged AUDIT doc — list each shared file, the flows that touch it, and a single proposed fix that resolves all referenced gaps.
5. Mark referenced per-flow gap entries with `[CROSS-FLOW: see X]` so they're not fixed twice in Phase 29+.

**Step 1.2 — Per-flow merge** into `.planning/milestones/v5.0-AUDIT.md` using this structure (model on `v2.0-AUDIT.md`):

```markdown
# v5.0 Feature Completion & E2E Polish — Milestone Audit

**Audited:** 2026-04-19 (complete post-Wave-1)
**Status:** AUDIT COMPLETE — N gaps identified across 10 flows
**Pattern:** v1.1 gap-closure — audit -> identify -> close

---

## Executive Summary

- Total gaps: N (HIGH: x / MED: y / LOW: z)
- B-flow totals: ...
- D-flow totals: ...
- Proposed Phase 29+ phases: M
- OPS-02 carry-forward dependencies flagged: <list>

## Requirements Coverage Matrix

| # | Requirement | Phase | Status | Evidence |
|---|-------------|-------|--------|----------|
| B1 | Quick check-in <10s E2E | 29+ | GAP (N items) | see Gap Inventory |
| ... | ... | ... | ... | ... |

## Cross-Flow Integration Gaps

Gaps that span multiple flows (e.g., share pipeline used by D1, D2, D3) get their own section so they don't get fixed 3 times.

## Gap Inventory by Flow

### B1: Quick check-in <10s
<paste merged gap table from 28-01-FINDINGS.md>

...continue through B2-B5 and D1-D5...

## Proposed Phase 29+ Breakdown

See next task.
```

Verbatim-preserve evidence citations from the FINDINGS files. Do not rewrite gaps — merge and format.

If a FINDINGS file is missing sections or evidence, record that in an **Audit Integrity Issues** section at the top — do not silently fix.

> verification: test -f .planning/milestones/v5.0-AUDIT.md && grep -c '^### [BD][1-5]' .planning/milestones/v5.0-AUDIT.md
  </action>
  <verify>
test -f .planning/milestones/v5.0-AUDIT.md && grep -c '^### [BD][1-5]' .planning/milestones/v5.0-AUDIT.md
  </verify>
  <done>v5.0-AUDIT.md exists with all 10 flow sections + executive summary + coverage matrix. Reality Checker verifies every gap entry cites evidence from a FINDINGS file.</done>
</task>

<task type="manual">
  <name>Task 2: Design-resolution + group gaps into Phase 29+ candidates with severity-weighted sequencing</name>
  <files>.planning/milestones/v5.0-AUDIT.md</files>
  <action>
**Step 2.0 — Gap-count branch** (overflow guard):
- Count total unique gap entries (post cross-flow dedup).
- If total > 40: STOP. Surface to user: "Audit found {N} gaps, exceeds v1.1 envelope (~30). Choose: (a) expand v5.0 to {ceil(N/4)} phases, (b) defer LOW-severity to v5.1, (c) re-scope by dropping {flow}." Do not write Phase 29+ rows until user decides.
- If total <= 40: continue.

**Step 2.1 — Design resolution** (insert BEFORE Phase 29+ breakdown):
For every gap classified MISSING or PARTIAL, add a `**Desired behavior**:` line stating the explicit end-state — not just "implement the missing piece" but the concrete behavior the user/system should exhibit. Example open questions to resolve here:
- B5 large-venue radius: what's the target radius per `venue_type` (stadium / festival / club / outdoor)? Pick concrete meters values.
- B5 spoof detection: should it block check-in, warn user, silently flag for moderation, or all three?
- B4 "within N hours": pin a concrete N from the BullMQ cadence audit.
- D4 share-link domain: stay on `soundcheck-app.up.railway.app` or migrate to `getsoundcheck.app`?
- D1/D2 share targets: support all 3 (IG Stories / X / TikTok) or subset for v5.0?

If a design question requires user input, surface to user before completing this task — do NOT make product decisions silently.

**Step 2.2 — Append Proposed Phase 29+ Breakdown** to `v5.0-AUDIT.md`. Group the gap inventory into candidate phases following v1.1 gap-closure pattern (small phases, 1-3 plans each, clear dependencies).

Proposed format:

```markdown
## Proposed Phase 29+ Breakdown

### Phase 29: <name> (HIGH severity, no dependencies)
- **Goal**: close gaps <list of gap IDs>
- **Requirements satisfied**: <B/D IDs>
- **Depends on**: Phase 28 (this audit)
- **Plans estimate**: N
- **Scope**: <1-2 sentence description>

### Phase 30: <name>
...
```

Sequencing rules (apply in order):
1. **Cross-flow integration phase first**: any cross-flow surface (from Step 1.1) gets its own phase BEFORE per-flow phases — fixing it once unblocks multiple downstream phases.
2. HIGH-severity gaps without external dependencies next (Phases 30+).
3. Gaps tagged BLOCKED-OPS-02 grouped into a single phase flagged **BLOCKED-OPS-02** — do not schedule until providers configured.
4. MED-severity gaps grouped by shared files/services (avoid parallel edits to same file across phases).
5. LOW-severity / polish gaps batched into a final "polish sweep" phase.
6. Wave-2 / consolidation plan added to a phase if it touches 3+ services.
7. **Phase count derives from gap density**: target ~3-5 plans per phase. Total phase count = whatever satisfies that constraint. Do NOT pad or compress to hit a target number.
8. **Execution model defense**: in a 1-paragraph note at top of breakdown, defend the chosen execution model. Default = severity-batched per v1.1. Alternatives to consider: vertical-slice-per-flow (one phase = one flow E2E green) or domain-driven (B-domain phase, D-domain phase). Pick one and explain why for v5.0 specifically.

Add a summary table at the end of the section:

```markdown
| Phase | Name | Plans | Severity | Blocked? |
|-------|------|-------|----------|----------|
| 29 | <name> | N | HIGH | No |
| 30 | ... | ... | ... | ... |
```

Reality Checker verifies: total plan count across Phases 29+ equals total unique gap fixes from the gap inventory (no gaps lost, no double-counting).

> verification: grep -c '^### Phase [0-9]' .planning/milestones/v5.0-AUDIT.md
  </action>
  <verify>
grep -c '^### Phase [0-9]' .planning/milestones/v5.0-AUDIT.md
  </verify>
  <done>Phase 29+ breakdown complete with phase count derived from gap density (not padded to a target), severity-weighted sequencing, cross-flow phase first, BLOCKED-OPS-02 phase flagged separately. Summary table at end. Execution model defense paragraph included.</done>
</task>

<task type="manual">
  <name>Task 3: Concretize REQUIREMENTS + update ROADMAP + update STATE</name>
  <files>.planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</files>
  <action>
Three sequential file updates — execute in this order:

**3a. `.planning/REQUIREMENTS.md`**:

Replace the 10 placeholder B1-B5 / D1-D5 entries with concrete, testable requirements derived from the gap inventory. Each concrete requirement:
- has a unique ID (B1-G1, B1-G2, ... or keep the gap IDs assigned in FINDINGS files)
- states observable behavior ("User checks in within 10s on first cold-start" not "B1 works")
- lists the code touch points (files/services) impacted
- includes a severity label matching v5.0-AUDIT.md

Mark AUDIT-01 as `[x]` satisfied (the audit doc exists).

**3b. `.planning/ROADMAP.md`**:

In the v5.0 section, after the Phase 28 block:
- Mark Phase 28 complete: `| 28. v5.0 AUDIT | v5.0 | 3/3 | Complete | 2026-04-19 |`
- Add rows for Phase 29+ per the breakdown in `v5.0-AUDIT.md`. Each row: number, name, 0/N plans, Pending status.
- Update the `Execution Order` line to include the new phase sequence.
- Update the milestone line: `🚧 v5.0 Feature Completion & E2E Polish — Phases 28-{last_new_phase_num}`.

**3c. `.planning/STATE.md`**:

Update the YAML frontmatter:
- `total_phases`: new count (Phase 28 + new phases)
- `completed_phases`: 1 (Phase 28)
- `total_plans`: new count (3 for Phase 28 + estimates for 29+)
- `completed_plans`: 3
- `last_updated`: current timestamp

Update the Current Position block:
- `Phase: 29 of {total}`
- `Phase Name: <Phase 29 name from AUDIT doc>`
- `Status: Pending -- ready for planning`
- `Last Activity: Phase 28 AUDIT complete (2026-04-19). v5.0-AUDIT.md published.`
- `Next Action: Run /legion:plan 29 to scope first gap-closure phase`

Update the Milestone block with post-audit counts and the list of Phase 29+ phases.

Append to Recent Decisions:
- `- Phase 28 complete (2026-04-19): v5.0-AUDIT.md published. N gaps identified (HIGH: x, MED: y, LOW: z). Phase 29+ seeded with M phases across ~N plans.`

Do NOT modify STATE.md during 3a or 3b — only after both complete, do 3c as the final commit.

Reality Checker verifies: REQUIREMENTS.md counts match AUDIT.md gap counts; ROADMAP.md Phase 29+ rows match AUDIT.md proposed breakdown exactly.

> verification: grep -c '^| 29\\.\\|^| 30\\.' .planning/ROADMAP.md
  </action>
  <verify>
grep -c '^| 29\\.\\|^| 30\\.' .planning/ROADMAP.md
  </verify>
  <done>REQUIREMENTS.md concretized with per-gap requirements. ROADMAP.md has Phase 29+ rows. STATE.md reflects Phase 28 complete and points to Phase 29.</done>
</task>

</tasks>

<verification>
- [ ] `.planning/milestones/v5.0-AUDIT.md` exists
- [ ] All 10 flow sections present (B1-B5, D1-D5)
- [ ] Executive summary + requirements coverage matrix + gap inventory + Phase 29+ breakdown
- [ ] Every gap claim traceable to 28-01-FINDINGS or 28-02-FINDINGS file:line
- [ ] `.planning/REQUIREMENTS.md` has concrete B/D requirements replacing placeholders
- [ ] `.planning/ROADMAP.md` has Phase 28 marked complete + Phase 29+ rows added
- [ ] `.planning/STATE.md` frontmatter + Current Position updated
- [ ] Reality Checker sign-off: no fantasy claims, every item evidence-backed
</verification>

<success_criteria>
- v5.0-AUDIT.md is an authoritative, evidence-backed gap inventory
- A developer opening Phase 29 knows exactly which files to touch and which behaviors to verify
- AUDIT-01 requirement satisfied
- Milestone v5.0 can now proceed to execution via `/legion:plan 29`
</success_criteria>

<output>
After completion:
- `.planning/phases/28-v5.0-audit/28-03-SUMMARY.md` documents the consolidation (gap count totals, Phase 29+ count, any findings integrity issues noted).
- 28-03-SUMMARY.md MUST include **Reality Checker Sampling Table** (N=10 random gaps from v5.0-AUDIT.md):

  | Sample | Gap ID | Cited Evidence | Inspected file:line | Verdict |
  |--------|--------|----------------|---------------------|---------|
  | 1 | B1-G2 | `checkin_screen.dart:142` | <what was actually there> | CONFIRMS / CONTRADICTS |
  | ... | ... | ... | ... | ... |

  If ANY row = CONTRADICTS: stop, flag the affected gap entry in v5.0-AUDIT.md, do NOT mark Phase 28 complete in STATE.md. Surface to user.

STATE updates expected after execution:
- Phase 28 marked complete (all 3 plans)
- AUDIT-01 satisfied
- v5.0-AUDIT.md path noted in STATE Milestone block
- Next Action: `/legion:plan 29`
</output>
