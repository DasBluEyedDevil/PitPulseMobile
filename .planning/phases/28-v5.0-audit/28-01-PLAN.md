---
phase: 28-v5.0-audit
plan: 01
type: audit
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-v5.0-audit/28-01-FINDINGS.md
files_forbidden:
  - backend/src/**
  - backend/migrations/**
  - mobile/lib/**
sequential_files: []
expected_artifacts:
  - .planning/phases/28-v5.0-audit/28-01-FINDINGS.md
autonomous: false
agents:
  - qa-verification-specialist
  - mobile-app-builder
requirements:
  - AUDIT-01
  - B1
  - B2
  - B3
  - B4
  - B5
verification_commands:
  - "test -f .planning/phases/28-v5.0-audit/28-01-FINDINGS.md && echo 'exists'"
  - "grep -c '^### B[1-5]' .planning/phases/28-v5.0-audit/28-01-FINDINGS.md"

must_haves:
  truths:
    - "FINDINGS.md covers all 5 B-flows (B1-B5) with per-flow h3 sections (### B1 ... ### B5)"
    - "Every gap claim backed by file path + line reference OR METHOD /path endpoint reference"
    - "Each gap classified as BROKEN / MISSING / PARTIAL / UNTESTED / DEGRADED per CONTEXT §2 evidence rubric"
    - "BROKEN classification includes reproduction evidence (curl output, log excerpt, test failure, or emulator screenshot); inferred-broken downgraded to UNTESTED"
    - "Each gap tagged with verification mode: STATIC-PASS / RUNTIME-VERIFIED / BLOCKED-OPS-02"
    - "Each gap assigned severity (HIGH / MED / LOW) per CONTEXT §4 concrete rubric and proposed fix sketch"
    - "Known tech debt (checkin_providers.dart / checkin_screen.dart createCheckIn stale ref) investigated and documented"
    - "QA Verification Specialist has final sign-off on every gap entry; partner agent contributes traces, QA validates evidence"
  artifacts:
    - ".planning/phases/28-v5.0-audit/28-01-FINDINGS.md"
  key_links: []
---

<objective>
Audit the Core Check-in E2E pipeline (flows B1-B5) and produce a findings document at `.planning/phases/28-v5.0-audit/28-01-FINDINGS.md` that enumerates every gap blocking end-to-end functionality. This is a **read-only audit phase** — do not modify any backend, migration, or mobile source files.

Every claim must cite `file:line` or a concrete API endpoint. Defaults are NEEDS WORK until evidence proves otherwise.

The consolidator (Plan 28-03) merges this output with 28-02-FINDINGS.md into `.planning/milestones/v5.0-AUDIT.md`.
</objective>

<context>
@.planning/phases/28-v5.0-audit/28-CONTEXT.md
@.planning/exploration-v5.0-feature-completion.md
@.planning/milestones/v2.0-AUDIT.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Audit B1 (Quick check-in <10s) + B2 (Photo upload E2E)</name>
  <files>.planning/phases/28-v5.0-audit/28-01-FINDINGS.md</files>
  <action>
Trace both flows end-to-end and capture gaps.

**B1 — Quick check-in <10s** (success = app open -> GPS -> event match -> rating -> submit -> visible in feed):

1. Mobile entry: open `mobile/lib/src/features/checkin/presentation/checkin_screen.dart`. Identify the "Check In" button handler and trace the provider chain through `checkin_providers.dart`. Confirm whether the known `createCheckIn()` stale reference (PROJECT.md tech debt) still exists. Note exact file:line.
2. HTTP layer: identify the repository / dio call made. Record endpoint path and payload shape.
3. Backend: trace `POST /api/checkins` (or current equivalent) -> `CheckinController` -> `CheckinCreatorService.createCheckin()`. Record file:line for each hop.
4. GPS auto-suggest: trace how `mobile/lib/src/core/services/location_service.dart` resolves nearby events. Verify the event-match endpoint used and what happens when no events are nearby.
5. Feed visibility: after a successful check-in, verify `FeedService.getFeed()` returns the new check-in on next pull. Confirm denormalized counts update.
6. Timing: identify any unnecessary network round-trips, blocking operations, or serial calls that would push total time past 10 seconds.

**B2 — Photo upload E2E** (success = camera/gallery -> presigned URL -> R2 PUT -> display on check-in card):

1. Mobile camera/gallery entry: identify the widget and permission flow.
2. Presigned URL request: trace `GET /api/uploads/presigned-url` (or current equivalent) -> `UploadService.ts`. Record file:line.
3. R2 PUT: verify the mobile side actually performs the PUT to the returned URL. Record the http client call.
4. Attach to check-in: verify the photo URL is persisted to `checkins.photo_urls` (or equivalent column). Record the column name and service method.
5. Display: verify feed cards render the uploaded photo (check `feed_card.dart` or equivalent widget). Verify R2 public URL shape matches what the card renders.
6. Known issue: `backend/tmp_r2_verify.js` exists in tree — investigate whether it indicates in-progress verification work or stale debug.

For each flow, write a section to `.planning/phases/28-v5.0-audit/28-01-FINDINGS.md` with this structure:

```markdown
### B1: Quick check-in <10s

**As-is state**: <short trace summary>

**Gaps identified**:

| ID | Gap | Classification | Severity | Evidence | Proposed Fix |
|----|-----|----------------|----------|----------|--------------|
| B1-G1 | <description> | BROKEN/MISSING/PARTIAL/UNTESTED/DEGRADED | HIGH/MED/LOW | `file:line` or `METHOD /path` | <1-2 sentence sketch + est. LOC> |

**No gaps found** sections OK if flow verifies clean.
```

Do not fix anything. Do not modify backend or mobile source. Read-only audit.

> verification: grep -c '^### B[12]' .planning/phases/28-v5.0-audit/28-01-FINDINGS.md
  </action>
  <verify>
grep -c '^### B[12]' .planning/phases/28-v5.0-audit/28-01-FINDINGS.md
  </verify>
  <done>B1 and B2 sections in FINDINGS.md with per-gap evidence and classification. Stale createCheckIn ref documented.</done>
</task>

<task type="manual">
  <name>Task 2: Audit B3 (Multi-band event check-in) + B4 (User-created event auto-merge)</name>
  <files>.planning/phases/28-v5.0-audit/28-01-FINDINGS.md</files>
  <action>
**B3 — Multi-band event check-in** (success = per-set band ratings capture correctly on multi-band lineups):

1. Event model: confirm events have a lineup of bands (`event_bands` or equivalent join table). Record schema.
2. Mobile UI: trace the multi-band rating widget in `checkin_screen.dart` or a sub-widget. Verify each band in the lineup gets its own rating control.
3. Payload: record the request body shape — does it send `band_ratings[]` with per-band ratings or a single `band_id + rating`?
4. Backend: trace `CheckinCreatorService` handling of multi-band payloads. Verify rows in `checkin_band_ratings` table (one row per rated band). Record file:line.
5. Edge cases: what happens if user rates 0 of N bands? All bands? A band not in the lineup? Does the backend accept or reject?
6. Feed/display: verify per-set ratings render on the feed card (not just a single rating).

**B4 — User-created event auto-merge** (success = manual event -> Ticketmaster fuzzy match -> merge within N hours):

1. Mobile UI: trace the "Create Event" flow (probably a fallback when GPS finds no match). Record entry point.
2. Backend: trace `EventService.createUserEvent()` or equivalent. Record how it writes to `events` table and any `is_user_created` / `source` flag.
3. Ticketmaster sync — full job lifecycle audit: trace the BullMQ queue + processor file. Document: (a) cadence (cron/interval), (b) retry/backoff config, (c) idempotency guarantees against double-merge, (d) dead-letter handling, (e) observability/alerting (winston/Sentry), (f) fuzzy-match algorithm + similarity thresholds. Recommend a concrete N value for "within N hours" v5.0 acceptance.
4. Merge logic: when a match is found, what happens? (merge check-ins? point `event_id` references to the Ticketmaster event? delete the user-created event?) Record file:line.
5. User-visible state change: notification, feed update, or silent? When does mobile reflect the merge?
6. Edge cases: duplicate events not matched, ambiguous matches (2+ candidates), match confidence thresholds.

Append B3 and B4 sections to `28-01-FINDINGS.md` using the same table format as Task 1.

Do not fix anything. Read-only.

> verification: grep -c '^### B[34]' .planning/phases/28-v5.0-audit/28-01-FINDINGS.md
  </action>
  <verify>
grep -c '^### B[34]' .planning/phases/28-v5.0-audit/28-01-FINDINGS.md
  </verify>
  <done>B3 and B4 sections in FINDINGS.md with per-gap evidence. BullMQ lifecycle (cadence/retry/idempotency/DLQ/observability/algorithm) documented. Concrete N value for "within N hours" recommended.</done>
</task>

<task type="manual">
  <name>Task 3: Audit B5 (Location verify edge cases)</name>
  <files>.planning/phases/28-v5.0-audit/28-01-FINDINGS.md</files>
  <action>
**B5 — Location verify edge cases** (success = no-GPS path, large venue radius, spoof detection, venue_type-aware radius all work correctly):

1. GPS off / denied: trace what `checkin_screen.dart` does when GPS permission denied or location unavailable. Is check-in blocked? Is there a manual venue-select fallback? Record file:line.
2. Radius configuration: find the current radius value(s) for check-in proximity. Is it a single constant, env var, or per-venue-type (stadium vs. club vs. outdoor festival)?
3. Large venue radius: stadiums and festivals have huge footprints. Does the current logic handle this? Record any venue_type-aware logic (or its absence).
4. Spoof detection: does the backend or mobile attempt to detect GPS spoofing (e.g., mock location flag on Android, implausibly fast movement between check-ins, IP geolocation cross-check)? Record what exists.
5. Non-blocking verification: PROJECT.md claims location verify is "non-blocking, configurable radius". Confirm — can a user still check in if outside the radius? What's the UX (warning, silent flag, denied)?
6. Backend: trace `LocationService.verifyLocation()` or equivalent. Record file:line for each decision branch.
7. **Stale-debug sweep** (bonus): scan `backend/` and `mobile/` for `tmp_*`, `scratch_*`, `_debug.*` files (e.g., `backend/tmp_r2_verify.js` already on tree). Flag any found.

Append B5 section to `28-01-FINDINGS.md` using the same table format.

Then add a closing **Summary Counts** section:

```markdown
## B-Flow Summary

| Flow | Gaps Found | Highest Severity |
|------|------------|------------------|
| B1 | N | HIGH/MED/LOW |
| B2 | N | HIGH/MED/LOW |
| B3 | N | HIGH/MED/LOW |
| B4 | N | HIGH/MED/LOW |
| B5 | N | HIGH/MED/LOW |
| **Total** | N | — |
```

Do not fix anything. Read-only audit.

> verification: grep -c '^### B5' .planning/phases/28-v5.0-audit/28-01-FINDINGS.md
  </action>
  <verify>
grep -c '^### B5' .planning/phases/28-v5.0-audit/28-01-FINDINGS.md
  </verify>
  <done>B5 section and B-flow summary counts in FINDINGS.md. All 5 B-flow sections present with evidence-backed gaps.</done>
</task>

</tasks>

<verification>
- [ ] `.planning/phases/28-v5.0-audit/28-01-FINDINGS.md` exists
- [ ] Contains 5 sections `### B1` through `### B5`
- [ ] Every gap entry includes file:line or endpoint evidence
- [ ] Every gap classified (BROKEN/MISSING/PARTIAL/UNTESTED/DEGRADED)
- [ ] Every gap has severity (HIGH/MED/LOW) and proposed fix sketch
- [ ] Summary table at end counts gaps per flow
- [ ] No backend or mobile source files modified
</verification>

<success_criteria>
- FINDINGS.md is a trustworthy input to Plan 28-03 consolidation
- Every gap claim is falsifiable (someone can open the cited file:line and confirm)
- `checkin_providers.dart` / `checkin_screen.dart` stale `createCheckIn` reference investigated
- Known tech debt from PROJECT.md confirmed or rebutted with code evidence
</success_criteria>

<output>
After completion, the findings file `.planning/phases/28-v5.0-audit/28-01-FINDINGS.md` is ready for Plan 28-03 consolidation. Do NOT create a SUMMARY.md yet — consolidation plan handles that.

**Do NOT modify ROADMAP.md or STATE.md from this plan** — Plan 28-03 is the sole writer to those files (avoids Wave 1 race with Plan 28-02). This plan only writes its FINDINGS.md.
</output>
