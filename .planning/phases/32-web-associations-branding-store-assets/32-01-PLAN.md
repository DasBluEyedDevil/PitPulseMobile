# Phase 32: Web Associations, Branding, and Store Assets

## Scope

Repair release-dependent association files, public page assets, store visuals, mobile asset declarations, and formatting baselines.

## Finding-to-test traceability

| Finding | Change | Required evidence |
| --- | --- | --- |
| MOB-OB-005 | Version `assetlinks.json` from verified release fingerprint | Served association response and signed-device link verification. |
| MOB-OB-006 | Capture iOS store screenshots | Dimension/content review and App Store upload preview. |
| MOB-OB-007 | Replace stale Android screenshots | Release-candidate visual comparison and Play preview. |
| MOB-OB-008 | Generate SoundCheck platform icons | Dimension/alpha/safe-zone checks and signed-install inspection. |
| MOB-OB-009 | Apply Dart format and gate it | Pinned formatter clean check. |
| MOB-OB-010 | Remove unreachable bundled brand assets | Asset-manifest/size comparison. |
| OB-COORD-003 | Repair public image references/content types | Built-page and header/content-type probes. |
| OB-COORD-005 | Apply backend Prettier and gate it | `npm run format:check` clean check. |

## Acceptance criteria

- Association files use verified identities/fingerprints and public lifecycle pages build without missing assets.
- Store graphics accurately depict the release candidate and only runtime assets ship in Flutter bundles.
- Formatter gates pass without mutating source.

## Evidence boundaries

Static association correctness is not device verification, and local screenshot review is not proof of remote listing state.

## Deployment and rollback

Publish web assets atomically with cache-aware verification. Roll back static deploys to the prior immutable release; retain canonical store artwork provenance.

## Local remediation evidence (2026-07-26)

| Finding | Local result | Evidence |
| --- | --- | --- |
| MOB-OB-005 | Partially remediated, externally blocked | `npm run validate:aasa --prefix web` passes for `BDJDR669ZV.com.9thlevelsoftware.soundcheck`. Android generation and validation fail closed unless `SOUNDCHECK_ANDROID_VERIFIED_APP_SIGNING_SHA256` is supplied; no fingerprint or placeholder `assetlinks.json` was fabricated. |
| MOB-OB-006/007 | Stale upload set removed; capture blocked | The five lime-era Android files were removed from `android/curated`. `inventory.json` defines five corresponding Android/iOS journeys and requires a signed build, exact candidate SHA, device/OS review, and distinct portrait PNGs. `npm run fastlane:check` intentionally fails while both signed-candidate sets are absent. |
| MOB-OB-008 | Deterministic local generation passes | `npm run generate:platform-icons --prefix web` derives the Android adaptive/legacy sets and all iOS catalog images from `flash_mark_cutout.png`. `npm run validate:platform-icons --prefix web` passes 15 Android PNGs, two adaptive XMLs, 19 iOS catalog entries across 15 opaque PNGs, provenance hash/identities, dimensions, alpha policy, and the 66dp adaptive safe zone. |
| MOB-OB-009 | Mechanical formatter baseline passes | Commit `14c97b5` applied `dart format` without semantic edits. The required check is stable over 237 Dart files, `flutter analyze` reports no issues, and the bounded full mobile suite passes `186/186`. Linux/macOS/Windows registrant line-ending noise was verified and restored because desktop targets are outside this release. |
| MOB-OB-010 | Local source and built bundle pass | `npm run check:mobile-assets` and `flutter build bundle && node ../scripts/check-mobile-runtime-assets.mjs --built` pass with 15 referenced runtime files (2,813,992 source bytes) out of 22,002,302 tracked artwork bytes. Source, generated, alternate, and legacy artwork moved to `mobile/store-assets/design/`; it remains tracked but is outside the Flutter asset tree and bundle allowlist. |
| OB-COORD-003 | Local built surface passes | Header sync now copies the valid PNG wordmark instead of retaining JPEG data under a PNG name. `npm run build --prefix web` builds eight pages; the post-build validator passes reset-password, delete-account, support, privacy, terms, child-safety, download, favicon/OG/header PNG signatures, redirects, AASA, and association content-type declarations. A local Astro preview returned 200 HTML for all eight routes, `image/png` for favicon/OG/header, and 200 for AASA; Astro preview does not apply the deployment `_headers` file, so deployed AASA content type remains an external probe. Backend `npm run test:runtime-assets --prefix backend` passes 6/6 built Wrapped/share/fallback/content-type probes. |
| OB-COORD-005 | Mechanical formatter baseline passes | Commit `14c97b5` applied backend Prettier without semantic edits; `a542f3a` made eight test-only lint suppressions formatter-stable. `npm run format:check`, TypeScript, ESLint with zero errors, the backend build/runtime validator, and 54 suites/616 tests pass with the CI JWT fixture. The existing 315-warning ESLint debt remains non-blocking under the versioned CI policy. |

## External evidence still required

- Obtain the independently verified Android Play App Signing/release certificate SHA-256 fingerprint, generate and version `assetlinks.json`, deploy it as JSON without redirect, and reproduce App Links on a signed install. Local upload-key evidence must not substitute for Play App Signing.
- Capture the five inventory journeys from the exact signed Android and iOS release candidate, record its immutable SHA, review minimum/current OS plus phone/tablet output, preview both store listings, and change inventory status only after approval.
- Inspect Android adaptive/legacy and iOS icons on signed installed builds. Local image dimensions and safe-zone checks do not prove launcher masking or App Store processing.
- Probe the deployed public lifecycle/legal/association URLs and deployed backend Wrapped/share fixtures for status, redirect, cache, content type, and deep-link behavior.
