# Phase 31: Mobile Session and Release Correctness

## Scope

Unify authenticated-session initialization and reconcile iOS commercial/release identity configuration.

## Finding-to-test traceability

| Finding | Change | Required evidence |
| --- | --- | --- |
| MOB-OB-012 | One post-auth session bootstrap | Password, registration, Google, Apple, restoration, logout, and account-switch tests. |
| MOB-OB-003 | Reconcile all remaining iOS identity consumers | Firebase/AASA/RevenueCat/docs consistency plus archive/upload evidence. |
| MOB-OB-004 | Confirm release entitlement behavior | Xcode archive effective-entitlement inspection and production APNs delivery. |

## Acceptance criteria

- Every successful authentication path invokes one observable, retry-safe session bootstrap exactly once.
- A valid login and known entitlement survive transient realtime/subscription synchronization failures.
- Android and iOS identities are documented and consumed consistently.

## Evidence boundaries

Mocks cannot prove Apple/Google login, RevenueCat, APNs, or a signed archive. Those require non-secret sandbox/device evidence in Phase 34.

## Deployment and rollback

Release mobile changes through internal tracks first. Roll back by promoting the previously certified binary; preserve server-side entitlement and account records.

## Fix round 1: lifecycle race and cleanup hardening

- Reserve an authenticated-session generation before each login, registration,
  social completion, retry, restoration, or logout operation. Serialize
  lifecycle work and require a current generation after every asynchronous
  integration boundary before publishing user, entitlement, or bootstrap
  status.
- Fence WebSocket connection readiness and Firebase token registration with
  service-local generations so delayed work cannot restore a logged-out or
  superseded account.
- Keep the RevenueCat customer-info listener installed across identity and
  customer-info refresh, remove it explicitly during cleanup, and reconcile
  RevenueCat/server evidence conservatively through one generation-aware
  coordinator.
- Run account-transition and logout cleanup as independent, bounded steps.
  Preserve the prior push token for unregister retries, retry only failed
  logout steps once, record permanent cleanup degradation, and clear auth
  storage after the bounded attempt.
- Invalidate the complete catalog of current-user query and mutation providers
  before publishing a replacement account.
- Verify with real production service/default-integration seams in addition to
  orchestration fakes: delayed WebSocket/push/RevenueCat work, listener
  retention/removal, token unregister/register failures, cleanup isolation and
  retry, entitlement truth tables, provider inventory, and publication
  ordering.

## Fix round 2: pre-I/O fencing and multi-account cleanup

- Reserve the auth generation before every network, SDK, or storage await for
  password, registration, Google, Apple, restoration, and logout.
- Keep login/register/social responses side-effect free until `AuthState`
  serializes a generation-guarded credential commit. If a platform write
  completes after supersession, roll back the partial credential set inside
  the same serialized section before a newer commit can begin.
- Move Google and Apple SDK/backend orchestration under notifier-owned auth
  entry points. Keep `SocialAuthService` response-only behind an injectable
  SDK platform adapter.
- Replace the single pending transition marker with a per-user cleanup queue.
  A transition to C drains pending A and active B before C publication; logout
  performs one bounded retry of every pending account cleanup, reports any
  remaining degradation, and still clears local credentials.
- Route paywall, purchase/restore, customer-center, RevenueCat listener, and
  server entitlement results through the entitlement generation. Remove the
  public direct premium setter so stale session A evidence cannot promote or
  demote session B.
- Exercise the production composition through real `AuthState`,
  `AuthRepository`, `SocialAuthService`, default authenticated-session
  integrations, `PremiumPaywallSheet`, and default subscription client. Fake
  only Dio, secure-storage/platform channels, WebSocket/Firebase/RevenueCat SDK
  adapters, and deterministic preferences.
- Final local evidence: generated code completed, the 15 changed Dart/test
  files were formatter-stable, `flutter analyze` reported no issues, and the
  single bounded full mobile suite passed all 147 tests. Live provider and
  signed-device evidence remains in Phase 34.

## Fix round 3: authoritative credential visibility and logout durability

- Make one `AuthSessionStore` the authority for token, refresh-token, and user
  visibility. A write-ahead revision marker exposes only a complete tuple:
  `committing:<revision>` hides in-progress writes,
  `active:<revision>` exposes a tuple only when the exact marker is unchanged
  before and after every field read, and `loggedOut:<revision>` durably hides
  raw residue.
- Migrate complete legacy token/user tuples to an active revision under the
  same serialized writer queue used by login, refresh, profile updates, and
  logout. Reject incomplete legacy tuples and prevent migration from racing
  past a logout tombstone.
- Carry the initiating active revision across Dio refresh I/O. A delayed
  refresh for A cannot overwrite B, resurrect a logged-out session, or
  invalidate B when its revision has changed. Repository restoration,
  WebSocket bootstrap, Dio injection/retry, token refresh, profile updates,
  logout, and 401 clearing all use the same authority.
- Persist logout intent before publishing null. Retry raw-key cleanup once,
  expose remaining residue as `localCredentials` cleanup degradation, retain
  the published user if the tombstone itself cannot be written, and apply the
  same durable-invalidation rule to Dio 401 handling.
- Fence `refreshUser` with the captured session generation and initiating
  active user identity without advancing the lifecycle generation. Logout or
  login B supersedes delayed A, while a profile refresh started after user
  publication cannot cancel the remaining exactly-once bootstrap steps.
- Treat only RevenueCat's documented anonymous-user logout error as
  idempotent success. Propagate every other `PlatformException` so the default
  cleanup path can retry once and report permanent degradation.
- Production-composition tests exercise real `AuthState`, `AuthRepository`,
  `SocialAuthService`, and default authenticated-session integrations with
  only storage/network/WebSocket/Firebase/RevenueCat platform seams faked.
  Coverage includes write/delete/read failures, revision races, durable logout
  restoration and Dio behavior, RevenueCat retry/status, and successful
  registration, Google, Apple, and restoration bootstrap parity.
- Final local evidence: `flutter analyze` reported no issues; the focused
  auth/session/subscription aggregate passed 60 tests; and exactly one bounded
  full mobile suite passed all 171 tests. Live provider and signed-device
  evidence remains in Phase 34.

## Fix round 4: exact revision identity across final commits and retries

- A successful credential write now returns its immutable revision. If the
  initiating authentication is superseded after the final `active` marker
  write or after repository completion, only that exact revision is
  conditionally invalidated. A failed B login therefore cannot leave a
  superseded A session restorable or injectable.
- Repository persistence returns `AuthPersistenceResult` directly without a
  shared last-revision field. The legacy boolean method remains a wrapper, so
  concurrent repository callers cannot exchange revision identities.
- Profile updates capture A's initiating session, pin the outbound request to
  A's exact token/revision, and conditionally commit the returned user JSON
  only while A remains active. A delayed A response cannot combine B's token
  with A's profile or overwrite B's stored user.
- Dio records the exact initiating revision on every authenticated request.
  Failed refresh and 401 invalidation are conditional on that revision;
  successful refresh returns the exact replacement session and retries with
  its token only while that replacement revision remains active. A request is
  never replayed with a newer account's token.
- Idempotent GET retries recheck the initiating revision before and after
  backoff and preserve the original authenticated or unauthenticated request
  identity. Non-idempotent requests are not replayed after a stale refresh.
- Authoritative session reads validate the complete persisted `User` shape.
  Malformed legacy JSON and malformed active tuples fail closed, are not
  injected into Dio, and cannot restore an authenticated state.

### Round-4 regression and verification evidence

- The independent round-3 review supplied deterministic source/call-path RED
  reproductions for the final-marker, profile, refresh, and malformed-session
  races. The implementation was corrected and matching production-path
  regressions were then added; no pre-fix test run is claimed for this round.
- Production-path regressions use real `AuthState`, `AuthRepository`,
  `DioClient`, and `AuthSessionStore` with only secure-storage and HTTP seams.
  They cover final-marker and caller-continuation supersession, B-login
  failure, profile interception and delayed response, failed and successful
  refresh races, non-idempotent replay prevention, and malformed legacy and
  active tuples.
- The focused auth production/state/login aggregate passed `66/66`.
- `flutter analyze` reported no issues, and the final bounded full
  `flutter test --reporter compact` run passed `178/178`.
- The seven Linux/macOS/Windows generated registrants changed only by line
  endings during Flutter tooling, were verified with
  `git diff --ignore-space-at-eol`, and were restored because desktop targets
  are outside this mobile release scope.
- The production raw-key audit found auth token, refresh token, and user tuple
  access only in `AuthSessionStore`; `ApiConfig` contains only key constants.
  Live identity-provider, RevenueCat dashboard/webhook, APNs, signed archive,
  and device evidence remains Phase 34 scope.
