# Plan 20-03: Vibes Reduction — Show 6 with Expandable "More Vibes"

**Phase**: 20 — UX Restructuring
**Requirements**: UIUX-27
**Depends on**: Nothing (independent of other Phase 20 plans)
**Estimated tasks**: 2

## Goal

The check-in vibes selector shows 6 vibes initially (the 6 most universally relevant) with a "More vibes" chip that expands to reveal the remaining 4, reducing Hick's Law decision paralysis from 10 simultaneous choices to 6.

## Context

**What exists:**
- `mobile/lib/src/features/checkins/presentation/checkin_screen.dart` lines ~58-69: `_vibeOptions` list of 10 vibes, all rendered simultaneously
- `checkin_screen.dart` lines ~2028-2083: `_VibeSelector` widget — `Wrap` with `spacing: 8, runSpacing: 8` rendering all vibes from the passed `vibes` list
- `checkin_screen.dart` lines ~1282-1286: `_VibeSelector` called with `vibes: _vibeOptions` — passes all 10 vibes

**What's wrong:**
- 10 vibes displayed simultaneously is a Hick's Law violation — too many choices at once slows decision-making and increases cognitive load
- No progressive disclosure — user sees the full set immediately without the option to see fewer

**Approach:** Reorder `_vibeOptions` to put the 6 most universally relevant vibes first (Great Sound, Good Vibes, Epic Lighting, Packed House, Intimate, Singing Along), then the 4 niche vibes (Mosh Pit, Crowd Surfing, Headbanging, Pyro). Convert `_VibeSelector` from a stateless widget to a stateful widget with `_expanded` state. Show first 6 vibes + a "More vibes" chip when collapsed; show all 10 when expanded.

## Tasks

### Task 1: Reorder _vibeOptions — universal vibes first, niche vibes last (UIUX-27)

**File**: `mobile/lib/src/features/checkins/presentation/checkin_screen.dart`

Reorder the list so the 6 most broadly applicable vibes come first.

**Lines ~58-69** — reorder vibes:
```dart
// Before:
  final List<Map<String, String>> _vibeOptions = [
    {'id': 'mosh_pit', 'name': 'Mosh Pit', 'icon': '\u{1F918}'},
    {'id': 'crowd_surfing', 'name': 'Crowd Surfing', 'icon': '\u{1F3C4}'},
    {'id': 'great_sound', 'name': 'Great Sound', 'icon': '\u{1F50A}'},
    {'id': 'epic_lighting', 'name': 'Epic Lighting', 'icon': '\u{2728}'},
    {'id': 'intimate', 'name': 'Intimate', 'icon': '\u{1F56F}'},
    {'id': 'packed', 'name': 'Packed House', 'icon': '\u{1F465}'},
    {'id': 'good_vibes', 'name': 'Good Vibes', 'icon': '\u{270C}'},
    {'id': 'singing_along', 'name': 'Singing Along', 'icon': '\u{1F3A4}'},
    {'id': 'headbanging', 'name': 'Headbanging', 'icon': '\u{1F3B8}'},
    {'id': 'pyro', 'name': 'Pyro', 'icon': '\u{1F525}'},
  ];

// After:
  final List<Map<String, String>> _vibeOptions = [
    // Primary vibes (shown initially)
    {'id': 'great_sound', 'name': 'Great Sound', 'icon': '\u{1F50A}'},
    {'id': 'good_vibes', 'name': 'Good Vibes', 'icon': '\u{270C}'},
    {'id': 'epic_lighting', 'name': 'Epic Lighting', 'icon': '\u{2728}'},
    {'id': 'packed', 'name': 'Packed House', 'icon': '\u{1F465}'},
    {'id': 'intimate', 'name': 'Intimate', 'icon': '\u{1F56F}'},
    {'id': 'singing_along', 'name': 'Singing Along', 'icon': '\u{1F3A4}'},
    // Expanded vibes (behind "More vibes")
    {'id': 'mosh_pit', 'name': 'Mosh Pit', 'icon': '\u{1F918}'},
    {'id': 'crowd_surfing', 'name': 'Crowd Surfing', 'icon': '\u{1F3C4}'},
    {'id': 'headbanging', 'name': 'Headbanging', 'icon': '\u{1F3B8}'},
    {'id': 'pyro', 'name': 'Pyro', 'icon': '\u{1F525}'},
  ];
```

### Task 2: Convert _VibeSelector to show 6 vibes + "More vibes" expandable chip (UIUX-27)

**File**: `mobile/lib/src/features/checkins/presentation/checkin_screen.dart`

Convert `_VibeSelector` from `StatelessWidget` to `StatefulWidget` with `_expanded` boolean. Show first 6 vibes when collapsed, all vibes when expanded. Add a "More vibes" chip at the end when collapsed. If any of the hidden vibes (indices 6-9) are already selected, auto-expand.

**Lines ~2028-2083** — replace `_VibeSelector`:
```dart
// Before:
class _VibeSelector extends StatelessWidget {
  const _VibeSelector({
    required this.vibes,
    required this.selectedVibes,
    required this.onToggle,
  });

  final List<Map<String, String>> vibes;
  final Set<String> selectedVibes;
  final Function(String) onToggle;

  @override
  Widget build(BuildContext context) {
    return Wrap(
      spacing: 8,
      runSpacing: 8,
      children: vibes.map((vibe) {
        final isSelected = selectedVibes.contains(vibe['id']);
        return GestureDetector(
          onTap: () => onToggle(vibe['id']!),
          child: Container(
            padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 10),
            decoration: BoxDecoration(
              color: isSelected
                  ? AppTheme.electricPurple
                  : AppTheme.surfaceVariantDark,
              borderRadius: BorderRadius.circular(20),
              border: isSelected
                  ? null
                  : Border.all(
                      color: AppTheme.textTertiary.withValues(alpha: 0.3),),
            ),
            child: Row(
              mainAxisSize: MainAxisSize.min,
              children: [
                Text(vibe['icon']!, style: const TextStyle(fontSize: 14)),
                const SizedBox(width: 6),
                Text(
                  vibe['name']!,
                  style: TextStyle(
                    color: isSelected
                        ? AppTheme.backgroundDark
                        : AppTheme.textSecondary,
                    fontWeight:
                        isSelected ? FontWeight.w600 : FontWeight.w500,
                    fontSize: 13,
                  ),
                ),
              ],
            ),
          ),
        );
      }).toList(),
    );
  }
}

// After:
class _VibeSelector extends StatefulWidget {
  const _VibeSelector({
    required this.vibes,
    required this.selectedVibes,
    required this.onToggle,
  });

  final List<Map<String, String>> vibes;
  final Set<String> selectedVibes;
  final Function(String) onToggle;

  @override
  State<_VibeSelector> createState() => _VibeSelectorState();
}

class _VibeSelectorState extends State<_VibeSelector> {
  static const int _initialCount = 6;
  bool _expanded = false;

  @override
  Widget build(BuildContext context) {
    // Auto-expand if any hidden vibe is already selected
    final hasHiddenSelection = widget.vibes.length > _initialCount &&
        widget.vibes
            .skip(_initialCount)
            .any((v) => widget.selectedVibes.contains(v['id']));
    final showAll = _expanded || hasHiddenSelection;
    final visibleVibes =
        showAll ? widget.vibes : widget.vibes.take(_initialCount).toList();

    return Wrap(
      spacing: 8,
      runSpacing: 8,
      children: [
        ...visibleVibes.map((vibe) {
          final isSelected = widget.selectedVibes.contains(vibe['id']);
          return GestureDetector(
            onTap: () => widget.onToggle(vibe['id']!),
            child: Container(
              padding:
                  const EdgeInsets.symmetric(horizontal: 14, vertical: 10),
              decoration: BoxDecoration(
                color: isSelected
                    ? AppTheme.electricPurple
                    : AppTheme.surfaceVariantDark,
                borderRadius: BorderRadius.circular(20),
                border: isSelected
                    ? null
                    : Border.all(
                        color:
                            AppTheme.textTertiary.withValues(alpha: 0.3),
                      ),
              ),
              child: Row(
                mainAxisSize: MainAxisSize.min,
                children: [
                  Text(vibe['icon']!,
                      style: const TextStyle(fontSize: 14)),
                  const SizedBox(width: 6),
                  Text(
                    vibe['name']!,
                    style: TextStyle(
                      color: isSelected
                          ? AppTheme.backgroundDark
                          : AppTheme.textSecondary,
                      fontWeight:
                          isSelected ? FontWeight.w600 : FontWeight.w500,
                      fontSize: 13,
                    ),
                  ),
                ],
              ),
            ),
          );
        }),
        // "More vibes" chip when collapsed and there are hidden vibes
        if (!showAll && widget.vibes.length > _initialCount)
          GestureDetector(
            onTap: () => setState(() => _expanded = true),
            child: Container(
              padding:
                  const EdgeInsets.symmetric(horizontal: 14, vertical: 10),
              decoration: BoxDecoration(
                color: AppTheme.surfaceVariantDark,
                borderRadius: BorderRadius.circular(20),
                border: Border.all(
                  color: AppTheme.electricPurple.withValues(alpha: 0.5),
                ),
              ),
              child: const Row(
                mainAxisSize: MainAxisSize.min,
                children: [
                  Icon(Icons.add, color: AppTheme.electricPurple, size: 16),
                  SizedBox(width: 4),
                  Text(
                    'More vibes',
                    style: TextStyle(
                      color: AppTheme.electricPurple,
                      fontWeight: FontWeight.w600,
                      fontSize: 13,
                    ),
                  ),
                ],
              ),
            ),
          ),
      ],
    );
  }
}
```

## Verification

After all tasks:
1. `dart analyze` — no errors on changed files
2. `flutter build apk --debug` — builds successfully
3. Grep `great_sound` in `checkin_screen.dart` confirms it appears at the first position in `_vibeOptions`
4. Grep `mosh_pit` in `checkin_screen.dart` confirms it appears after position 6 (after `singing_along`)
5. Grep `_initialCount = 6` in `checkin_screen.dart` confirms the visible threshold
6. Grep `More vibes` in `checkin_screen.dart` confirms the expandable chip text exists
7. Grep `_expanded` in `checkin_screen.dart` confirms the expand state variable exists
8. **Global sweep**: `grep -r "_VibeSelector" mobile/lib/src/features/checkins/` — should appear exactly 2 times (class def + call site)
9. Visual check: Check-in form shows 6 vibe chips + "More vibes" chip; tapping "More vibes" reveals 4 additional vibes; selecting a hidden vibe auto-expands the section

## Files Changed

| File | Action |
|------|--------|
| `mobile/lib/src/features/checkins/presentation/checkin_screen.dart` | MODIFY — reorder _vibeOptions (universal first, niche last); convert _VibeSelector from StatelessWidget to StatefulWidget with _expanded state; show 6 vibes + "More vibes" chip initially, full 10 when expanded |
