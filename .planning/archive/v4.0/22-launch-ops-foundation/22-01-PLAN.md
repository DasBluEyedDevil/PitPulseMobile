---
phase: 22-launch-ops-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
files_forbidden:
  - backend/.env
  - backend/.env.example
  - backend/DEPLOYMENT.md
  - backend/migrations/**
  - backend/src/**
  - mobile/**
  - railway.toml
  - nixpacks.toml
sequential_files: []
expected_artifacts:
  - path: ".planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
    provides: "Sanitized production launch-ops evidence covering secret rotation, Railway env parity, migration 039, demo seed, and verification results"
    required: true
autonomous: false
agents:
  - engineering-devops-automator
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
user_setup:
  - "Railway production dashboard or CLI access with permission to edit service variables and open a production shell"
  - "Provider access needed to rotate the PostgreSQL password and regenerate the SetlistFM API key"
  - "A secure off-repo location for the new DB password, JWT secret, and regenerated API key"
  - "Production admin credentials or JWT if the optional Sentry test route will be exercised"
verification_commands:
  - "test -f .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
  - "grep -q 'OPS-01: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
  - "grep -q 'OPS-02: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
  - "grep -q 'OPS-03: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
  - "grep -q 'NODE_ENV=production' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
  - "grep -q 'Migration 039: applied' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
  - "grep -q 'Demo seed: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
  - "grep -q 'Health check: 200' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
  - "grep -q 'No secret values recorded' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"

must_haves:
  truths:
    - "The evidence file records completion status and timestamps for rotating the DB password, JWT secret, and SetlistFM key, but never stores their values"
    - "The evidence file records Railway environment parity for NODE_ENV and all required third-party integrations, with any remaining blockers explicitly called out"
    - "The evidence file records how migration 039 was applied in production and what proof was used to confirm it"
    - "The evidence file records whether demo seeding required the base `npm run seed` fallback before `npm run seed:demo` could succeed"
    - "The evidence file records post-change launch verification outcomes for `/health`, authentication behavior, and optional Sentry validation"
  artifacts:
    - path: ".planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
      provides: "Sanitized completion record for OPS-01, OPS-02, and OPS-03"
      min_lines: 35
      contains: "OPS-03: complete"
  key_links:
    - from: ".planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
      to: "backend/.env.example"
      via: "Railway variable checklist and required integration inventory"
      pattern: "NODE_ENV=production"
    - from: ".planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
      to: "backend/DEPLOYMENT.md"
      via: "Post-deploy verification procedure and Sentry validation steps"
      pattern: "Health check: 200"
    - from: ".planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
      to: "backend/migrations/039_replace-social-auth-sentinel.ts"
      via: "Production migration application proof"
      pattern: "039_replace-social-auth-sentinel"
    - from: ".planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md"
      to: "backend/src/scripts/seed-demo.ts"
      via: "Demo account population and fallback handling"
      pattern: "Demo seed: complete"
---

<objective>
Close the remaining launch-ops blockers by rotating exposed production secrets, bringing Railway environment variables to parity, applying migration 039, populating demo data, and recording sanitized verification evidence.

Purpose: Phase 23 assumes production is stable and correctly configured. This plan clears the human-needed operational work that the repo and v2.0 audit already identified but did not execute.
Output: `.planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-launch-ops-foundation/22-CONTEXT.md

Relevant source files:
@backend/.env.example
@backend/DEPLOYMENT.md
@railway.toml
@backend/migrations/039_replace-social-auth-sentinel.ts
@backend/src/scripts/seed-demo.ts
@.planning/milestones/v2.0-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the sanitized launch-ops evidence record</name>
  <files>.planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md</files>
  <action>
Create `.planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md` before touching production. Use it as the single execution ledger for this phase.

The file must contain:
- A header identifying the Railway production service/environment, execution date, and operator.
- An `OPS-01` section with a table for `DB password`, `JWT_SECRET`, and `SETLISTFM_API_KEY` containing columns for `status`, `rotated_at`, `rotated_by`, and `rollback note`.
- An `OPS-02` section with a checklist for `NODE_ENV`, `DATABASE_URL`, `REDIS_URL`, `FIREBASE_SERVICE_ACCOUNT_JSON`, `CLOUDFLARE_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL`, `REVENUECAT_WEBHOOK_AUTH`, `RESEND_API_KEY`, `GOOGLE_CLIENT_ID`, `APPLE_BUNDLE_ID`, `BASE_URL`, `APP_STORE_URL`, `PLAY_STORE_URL`, `TICKETMASTER_API_KEY`, `SETLISTFM_API_KEY`, `SENTRY_DSN`, `CORS_ORIGIN` (if admin tools exist), and `ENABLE_WEBSOCKET`.
- An `OPS-03` section with placeholders for `Migration 039`, `Demo seed`, `Health check`, `Auth behavior`, `Feed auth guard`, and `Sentry verification`.
- A bold warning line: `No secret values recorded`.
- A blockers section for anything that prevents completion.

Populate the initial structure from `backend/.env.example`, `backend/DEPLOYMENT.md`, `railway.toml`, `backend/migrations/039_replace-social-auth-sentinel.ts`, and `backend/src/scripts/seed-demo.ts`. Do not paste any live secrets, DSNs, tokens, or database URLs into the file.

> verification: test -f .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'OPS-01' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'OPS-02' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'No secret values recorded' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
  </action>
  <verify>
test -f .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'OPS-01' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'OPS-02' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'No secret values recorded' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
  </verify>
  <done>The phase has a sanitized, execution-ready evidence ledger that captures completion and blockers without leaking secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Rotate exposed secrets and finish Railway environment parity</name>
  <files>.planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md</files>
  <action>
Perform the production configuration work tracked as `OPS-01` and `OPS-02`.

For `OPS-01`:
- Rotate the Railway PostgreSQL password from the Railway/Postgres provider side.
- Generate a new `JWT_SECRET` using the secure command documented in `backend/.env.example`.
- Regenerate the SetlistFM production API key from the provider account.
- Update Railway production variables to use the new values.
- Record only the completion status, timestamp, operator, and rollback note in the evidence file. Never write the new values to git-tracked files.

For `OPS-02`:
- Confirm `NODE_ENV=production`.
- Reconcile the live Railway production variable set against the Tier 1-3 list in `backend/.env.example` and the critical-variable list in `backend/DEPLOYMENT.md`.
- Mark each required integration variable as `configured`, `not needed`, or `blocked`.
- If an integration is intentionally deferred, document the justification in the blockers section rather than silently skipping it.

Mark `OPS-01: complete` only after the DB password, JWT secret, and SetlistFM key are all rotated and reflected in Railway. Mark `OPS-02: complete` only after the required variable audit is complete and `NODE_ENV=production` is confirmed.

> verification: grep -q 'OPS-01: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'OPS-02: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'DB password: rotated' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'JWT_SECRET: rotated' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'SETLISTFM_API_KEY: rotated' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'NODE_ENV=production' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'SENTRY_DSN: configured' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
  </action>
  <verify>
grep -q 'OPS-01: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'OPS-02: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'DB password: rotated' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'JWT_SECRET: rotated' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'SETLISTFM_API_KEY: rotated' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'NODE_ENV=production' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'SENTRY_DSN: configured' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
  </verify>
  <done>Production secrets are rotated, Railway variables are reconciled, and the evidence file explicitly records completion or blockers for OPS-01 and OPS-02.</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 039, populate demo data, and verify production readiness</name>
  <files>.planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md</files>
  <action>
Complete `OPS-03` using the already-defined deployment path and record the results in the same evidence file.

Execution sequence:
1. Ensure production is using the corrected variables from Task 2, then apply migration `039_replace-social-auth-sentinel`. Prefer the production service shell or the standard deploy path that runs `cd backend && npm run migrate:up && npm start`.
2. Record proof of migration application. Accepted proof is either:
   - deploy/shell output showing migration `039_replace-social-auth-sentinel` applied, or
   - a production database check confirming the migration is present in the migration tracking table.
3. Run `cd backend && npm run seed:demo` against production.
4. If `seed-demo.ts` reports missing venues or bands, treat that as a prerequisite failure, run `cd backend && npm run seed`, then rerun `cd backend && npm run seed:demo`. Record that fallback explicitly.
5. Run the post-change verification steps from `backend/DEPLOYMENT.md`:
   - `/health` returns 200 and reports a healthy database
   - invalid login attempt returns the expected auth response
   - `/api/feed` without auth still rejects access
   - optional `/api/debug/sentry-test` runs if an admin token is available; if not, document `Sentry verification: deferred with blocker`
6. Mark `OPS-03: complete` only after migration and demo seed succeed and the basic health/auth checks are recorded.

> verification: grep -q 'Migration 039: applied' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'Demo seed: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'Health check: 200' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'Feed auth guard: verified' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
> verification: grep -q 'OPS-03: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
  </action>
  <verify>
grep -q 'Migration 039: applied' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'Demo seed: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'Health check: 200' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'Feed auth guard: verified' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
grep -q 'OPS-03: complete' .planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md
  </verify>
  <done>Migration 039 is applied, demo data is populated, and production launch-readiness checks are captured in a sanitized evidence artifact.</done>
</task>

</tasks>

<verification>
- [ ] `.planning/phases/22-launch-ops-foundation/22-01-OPS-EVIDENCE.md` exists and contains sanitized execution records only
- [ ] `OPS-01: complete` is recorded with DB password, JWT secret, and SetlistFM key rotation evidence
- [ ] `OPS-02: complete` is recorded with `NODE_ENV=production` and required third-party variables reconciled
- [ ] `OPS-03: complete` is recorded with migration `039` proof and successful demo seed execution
- [ ] Health and auth verification outcomes are recorded, and any skipped Sentry test is explicitly called out as a blocker instead of omitted
</verification>

<success_criteria>
- Production secrets are rotated without storing any live values in the repository
- Railway production variables match the documented launch checklist closely enough to declare `OPS-02` complete or clearly blocked
- Migration `039_replace-social-auth-sentinel` is confirmed in production
- Demo data exists in production, with the base `npm run seed` fallback used if `seed:demo` prerequisites were missing
- The phase leaves a reusable evidence artifact that Phase 23 can trust as the production baseline
</success_criteria>

<output>
After completion, create `.planning/phases/22-launch-ops-foundation/22-01-SUMMARY.md`

STATE updates expected after execution:
- Mark Phase 22 as executed / pending review
- Carry forward any blockers that prevented full OPS completion
</output>
