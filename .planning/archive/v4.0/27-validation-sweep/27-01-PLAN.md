---
phase: 27-validation-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
files_forbidden:
  - backend/src/**
  - backend/migrations/**
  - mobile/**
sequential_files: []
expected_artifacts: []
autonomous: true
agents: []
requirements:
  - VAL-01
verification_commands:
  - "cd backend && npm test 2>&1 | grep -E 'Tests:|Test Suites:'"
  - "cd backend && grep -rn 'ReviewService\\|ReviewController\\|reviewRoutes\\|FROM reviews' src/ --include='*.ts' | grep -v __tests__ | wc -l"

must_haves:
  truths:
    - "Full backend test suite passes with zero failures"
    - "Smoke tests pass (health, auth rejection, feed guard)"
    - "Zero orphaned review references in production backend code"
    - "Admin, data export, and claim features have no reviews table dependencies"
  artifacts: []
  key_links: []
---

<objective>
Run comprehensive validation across the entire codebase to confirm all v4.0 changes are correct and nothing is broken. This is a read-only verification phase — no code changes.

Purpose: Final gate before marking v4.0 milestone complete.
Output: Validation report confirming all checks pass.
</objective>

<context>
@.planning/phases/27-validation-sweep/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full backend test suite</name>
  <files>none (read-only verification)</files>
  <action>
Run the complete backend test suite:
```
cd backend && npm test
```

Verify:
- Test Suites: all passing (0 failed)
- Tests: 0 failed
- Exit code: 0

If any tests fail, report them but do NOT fix them (this is a validation phase, not an execution phase).

> verification: cd backend && npm test 2>&1 | grep -E "Tests:|Test Suites:"
  </action>
  <verify>
cd backend && npm test 2>&1 | grep -E "Tests:|Test Suites:"
  </verify>
  <done>Full backend test suite passes with zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke tests and aggregate rating verification</name>
  <files>none (read-only verification)</files>
  <action>
**Smoke tests** — hit the production endpoint:
```
curl -s https://soundcheck-app.up.railway.app/health
curl -s -o /dev/null -w "%{http_code}" -X POST https://soundcheck-app.up.railway.app/api/users/login -H "Content-Type: application/json" -d '{"email":"fake@test.com","password":"wrong"}'
curl -s -o /dev/null -w "%{http_code}" https://soundcheck-app.up.railway.app/api/feed
```

Expected: health 200 (database connected), login 401, feed 401.

**Aggregate rating verification** — confirm the new rating queries reference checkin data:
```
grep -n "checkin_band_ratings\|checkins.*venue_rating" backend/src/services/VenueService.ts backend/src/services/BandService.ts
```

Should show the repointed queries (no reviews references).

> verification: curl -s -o /dev/null -w "%{http_code}" https://soundcheck-app.up.railway.app/health
  </action>
  <verify>
curl -s -o /dev/null -w "%{http_code}" https://soundcheck-app.up.railway.app/health
  </verify>
  <done>Smoke tests pass. Aggregate ratings verified using checkin data sources.</done>
</task>

<task type="auto">
  <name>Task 3: Orphaned review reference sweep</name>
  <files>none (read-only verification)</files>
  <action>
Sweep the entire backend for orphaned review references:

```
grep -rn "ReviewService\|ReviewController\|reviewRoutes\|FROM reviews\|INTO reviews\|review_helpfulness" backend/src/ --include="*.ts" | grep -v __tests__ | grep -v node_modules
```

Should return 0 matches (all review references eliminated).

Also check for any remaining imports:
```
grep -rn "import.*Review" backend/src/ --include="*.ts" | grep -v __tests__ | grep -v node_modules
```

Check mobile for orphaned review imports:
```
grep -rn "import.*review" mobile/lib/src/ --include="*.dart" | grep -v ".freezed." | grep -v ".g."
```

Check that the reviews directory is deleted:
```
test ! -d mobile/lib/src/features/reviews && echo "reviews dir deleted"
```

Verify admin/export/claim have no review dependencies:
```
grep -c "reviews" backend/src/controllers/AdminController.ts backend/src/services/DataExportService.ts || echo "0"
```

Note: `flutter analyze` is deferred (hangs in this environment). Document as known limitation.

> verification: cd backend && grep -rn "ReviewService\|ReviewController\|FROM reviews" src/ --include="*.ts" | grep -v __tests__ | wc -l
  </action>
  <verify>
cd backend && grep -rn "ReviewService\|ReviewController\|FROM reviews" src/ --include="*.ts" | grep -v __tests__ | wc -l
  </verify>
  <done>Zero orphaned review references. All v4.0 changes validated. VAL-01 satisfied.</done>
</task>

</tasks>

<verification>
- [ ] `npm test` passes with zero failures
- [ ] Production health endpoint returns 200
- [ ] Production auth rejection returns 401
- [ ] VenueService/BandService use checkin-based rating queries (no reviews)
- [ ] Zero orphaned review references in production code
- [ ] Mobile reviews directory deleted
- [ ] flutter analyze: deferred (environment limitation)
</verification>

<success_criteria>
- Full backend test suite passes
- Smoke tests pass
- Venue and band aggregate ratings verified correct from new data source
- Admin dashboard, data export, and claim features work without reviews table
- Zero orphaned references to deleted review system
</success_criteria>

<output>
After completion, create `.planning/phases/27-validation-sweep/27-01-SUMMARY.md`

STATE updates expected after execution:
- Mark Phase 27 plan 01 as complete
- Update VAL-01 requirement status
- Phase 27 status: complete
- v4.0 milestone: ready for completion via /legion:milestone
</output>
