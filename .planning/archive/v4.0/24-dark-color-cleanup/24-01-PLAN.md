---
phase: 24-dark-color-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/src/core/theme/app_theme.dart
  - mobile/lib/src/features/checkins/presentation/checkin_screen.dart
  - mobile/lib/src/features/checkins/presentation/checkin_detail_screen.dart
  - mobile/lib/src/features/checkins/presentation/photo_upload_sheet.dart
  - mobile/lib/src/features/checkins/presentation/rating_bottom_sheet.dart
  - mobile/lib/src/features/profile/presentation/profile_screen.dart
  - mobile/lib/src/features/profile/presentation/user_profile_screen.dart
  - mobile/lib/src/features/profile/presentation/blocked_users_screen.dart
  - mobile/lib/src/features/discover/presentation/discover_screen.dart
  - mobile/lib/src/features/feed/presentation/feed_screen.dart
  - mobile/lib/src/features/feed/presentation/providers/feed_providers.dart
  - mobile/lib/src/features/feed/presentation/widgets/feed_card.dart
  - mobile/lib/src/features/feed/presentation/widgets/happening_now_card.dart
  - mobile/lib/src/features/feed/presentation/widgets/new_checkins_banner.dart
  - mobile/lib/src/features/venues/presentation/venue_detail_screen.dart
  - mobile/lib/src/features/venues/presentation/widgets/venue_filters_sheet.dart
  - mobile/lib/src/features/bands/presentation/band_detail_screen.dart
  - mobile/lib/src/features/bands/presentation/widgets/band_filters_sheet.dart
  - mobile/lib/src/features/auth/presentation/login_screen.dart
  - mobile/lib/src/features/auth/presentation/register_screen.dart
  - mobile/lib/src/features/auth/presentation/forgot_password_screen.dart
  - mobile/lib/src/features/auth/presentation/reset_password_screen.dart
  - mobile/lib/src/features/verification/presentation/claim_submission_screen.dart
  - mobile/lib/src/features/verification/presentation/my_claims_screen.dart
  - mobile/lib/src/features/verification/presentation/widgets/owner_response_bottom_sheet.dart
  - mobile/lib/src/features/sharing/presentation/celebration_screen.dart
  - mobile/lib/src/features/sharing/presentation/share_card_preview.dart
  - mobile/lib/src/features/wrapped/presentation/wrapped_detail_screen.dart
  - mobile/lib/src/features/wrapped/presentation/wrapped_story_screen.dart
  - mobile/lib/src/features/wrapped/presentation/widgets/wrapped_slide.dart
  - mobile/lib/src/features/subscription/presentation/pro_feature_screen.dart
  - mobile/lib/src/features/subscription/presentation/widgets/premium_paywall_sheet.dart
  - mobile/lib/src/features/subscription/presentation/widgets/pro_badge.dart
  - mobile/lib/src/features/search/presentation/search_screen.dart
  - mobile/lib/src/features/search/presentation/discover_users_screen.dart
  - mobile/lib/src/features/events/presentation/event_detail_screen.dart
  - mobile/lib/src/features/events/presentation/friends_going_widget.dart
  - mobile/lib/src/features/events/presentation/rsvp_button.dart
  - mobile/lib/src/features/notifications/presentation/notifications_screen.dart
  - mobile/lib/src/features/onboarding/presentation/genre_picker_screen.dart
  - mobile/lib/src/features/trending/presentation/trending_feed_screen.dart
  - mobile/lib/src/features/reporting/presentation/widgets/report_bottom_sheet.dart
  - mobile/lib/src/features/badges/presentation/badge_collection_screen.dart
  - mobile/lib/src/shared/widgets/band_card_skeleton.dart
  - mobile/lib/src/shared/widgets/profile_skeleton.dart
  - mobile/lib/src/shared/widgets/scaffold_with_nav_bar.dart
  - mobile/lib/src/shared/widgets/skeleton_list.dart
  - mobile/lib/src/shared/widgets/venue_card_skeleton.dart
files_forbidden:
  - backend/**
  - mobile/lib/src/core/theme/app_theme.dart.bak
sequential_files:
  - mobile/lib/src/core/theme/app_theme.dart
expected_artifacts: []
autonomous: false
agents:
  - engineering-frontend-developer
requirements:
  - THEME-01
verification_commands:
  - "cd mobile && grep -r 'AppTheme\\.\\w*Dark' lib/ --include='*.dart' | wc -l | tr -d ' '"
  - "cd mobile && grep -r 'AppTheme\\.backgroundDark\\|AppTheme\\.cardDark\\|AppTheme\\.surfaceVariantDark\\|AppTheme\\.surfaceDark' lib/ --include='*.dart' -c"

must_haves:
  truths:
    - "Zero AppTheme.backgroundDark, AppTheme.cardDark, AppTheme.surfaceVariantDark, or AppTheme.surfaceDark references remain in mobile/lib/"
    - "The darkTheme ColorScheme includes surfaceContainerHigh and surfaceContainerHighest with the exact same hex values as cardDark and surfaceVariantDark (verifiable after Task 1/2, before Task 3 removes these constants)"
    - "All 47 files have been updated to use Theme.of(context) equivalents"
    - "The app_theme.dart static Dark constants are removed or marked deprecated"
    - "flutter analyze passes with no new errors introduced by these changes"
  artifacts: []
  key_links:
    - from: "mobile/lib/src/core/theme/app_theme.dart"
      to: "mobile/lib/src/features/*/presentation/*.dart"
      via: "ColorScheme properties replace static AppTheme.xxxDark constants"
      pattern: "Theme.of(context)"
---

<objective>
Remove all 253 AppTheme.*Dark color references across 47 Flutter files and replace them with theme-aware equivalents using Theme.of(context). Update the AppTheme infrastructure to support the migration.

Purpose: Hardcoded dark color references prevent light mode support. Converting to Theme.of(context) makes the app theme-switchable.
Output: Zero AppTheme.*Dark references. `flutter analyze` passes.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/24-dark-color-cleanup/24-CONTEXT.md

Key source file:
@mobile/lib/src/core/theme/app_theme.dart

Reference mapping:

- AppTheme.backgroundDark (93 refs) → Theme.of(context).scaffoldBackgroundColor
- AppTheme.cardDark (80 refs) → Theme.of(context).colorScheme.surfaceContainerHigh
- AppTheme.surfaceVariantDark (57 refs) → Theme.of(context).colorScheme.surfaceContainerHighest
- AppTheme.surfaceDark (23 refs) → Theme.of(context).colorScheme.surface
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AppTheme infrastructure for theme-aware colors</name>
  <files>mobile/lib/src/core/theme/app_theme.dart</files>
  <action>
Update the AppTheme class and darkTheme ThemeData to make the 4 Dark colors accessible via Theme.of(context).

**Step 1: Update ColorScheme in darkTheme**

In the `darkTheme` getter, update the `ColorScheme.dark(...)` to include the missing surface container mappings. Use the EXACT same hex values to avoid any visual regression:

```dart
ColorScheme.dark(
  // ... existing properties ...
  surface: surfaceDark,                    // 0xFF161B22 (already there)
  surfaceContainerHigh: cardDark,          // 0xFF1C2128 (NEW — replaces cardDark)
  surfaceContainerHighest: surfaceVariantDark, // 0xFF21262D (NEW — replaces surfaceVariantDark)
)
```

**Step 2: Verify scaffoldBackgroundColor**

Confirm that `scaffoldBackgroundColor: backgroundDark` is already set in the ThemeData. This is the replacement for `AppTheme.backgroundDark`.

**Step 3: Mark static constants as deprecated (do NOT remove yet)**

Add `@Deprecated('Use Theme.of(context) instead')` annotations to:

- `backgroundDark`
- `surfaceDark`
- `surfaceVariantDark`
- `cardDark`
- `background` (alias)
- `surfaceVariant` (alias)

Do NOT delete them yet — Task 2 needs them to exist during the transition. They'll be removed after all references are migrated.

> verification: cd mobile && grep -c "surfaceContainerHigh" lib/src/core/theme/app_theme.dart
> verification: cd mobile && grep -c "@Deprecated" lib/src/core/theme/app_theme.dart
  </action>
  <verify>
cd mobile && grep -c "surfaceContainerHigh" lib/src/core/theme/app_theme.dart
cd mobile && grep -c "@Deprecated" lib/src/core/theme/app_theme.dart
  </verify>
  <done>AppTheme.darkTheme ColorScheme includes surfaceContainerHigh and surfaceContainerHighest. Static Dark constants are deprecated.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 253 AppTheme.*Dark references across 47 files</name>
  <files>All 47 files listed in files_modified (excluding app_theme.dart)</files>
  <action>
Systematically replace every `AppTheme.*Dark` reference in the 47 feature/widget files with the correct Theme.of(context) equivalent.

**Replacement rules:**

1. `AppTheme.backgroundDark` → `Theme.of(context).scaffoldBackgroundColor`
2. `AppTheme.cardDark` → `Theme.of(context).colorScheme.surfaceContainerHigh`
3. `AppTheme.surfaceVariantDark` → `Theme.of(context).colorScheme.surfaceContainerHighest`
4. `AppTheme.surfaceDark` → `Theme.of(context).colorScheme.surface`

**Also replace the non-Dark aliases if present:**
5. `AppTheme.background` → `Theme.of(context).scaffoldBackgroundColor`
6. `AppTheme.surfaceVariant` → `Theme.of(context).colorScheme.surfaceContainerHighest`

**Execution strategy — work through files by directory, highest count first:**

1. **checkins/** (4 files, 55 refs): checkin_screen.dart, checkin_detail_screen.dart, photo_upload_sheet.dart, rating_bottom_sheet.dart
2. **profile/** (3 files, 30 refs): profile_screen.dart, user_profile_screen.dart, blocked_users_screen.dart
3. **feed/** (5 files, 24 refs): feed_screen.dart, feed_providers.dart, feed_card.dart, happening_now_card.dart, new_checkins_banner.dart
4. **discover/** (1 file, 20 refs): discover_screen.dart
5. **venues/** (3 files, 17 refs): venue_detail_screen.dart, venue_filters_sheet.dart + 1 more
6. **auth/** (4 files, 16 refs): login_screen.dart, register_screen.dart, forgot_password_screen.dart, reset_password_screen.dart
7. **verification/** (3 files, 15 refs): claim_submission_screen.dart, my_claims_screen.dart, owner_response_bottom_sheet.dart
8. **shared/widgets/** (5 files, 13 refs): band_card_skeleton.dart, profile_skeleton.dart, scaffold_with_nav_bar.dart, skeleton_list.dart, venue_card_skeleton.dart
9. **bands/** (2 files, 12 refs): band_detail_screen.dart, band_filters_sheet.dart
10. **sharing/** (2 files, 10 refs): celebration_screen.dart, share_card_preview.dart
11. **wrapped/** (3 files, 9 refs): wrapped_detail_screen.dart, wrapped_story_screen.dart, wrapped_slide.dart
12. **subscription/** (3 files, 7 refs): pro_feature_screen.dart, premium_paywall_sheet.dart, pro_badge.dart
13. **Remaining** (8 files, 25 refs): search, events, notifications, onboarding, trending, reporting, badges

**IMPORTANT contextual notes:**

- If a file uses `AppTheme.backgroundDark` as a `backgroundColor` on a `Scaffold`, prefer `Theme.of(context).scaffoldBackgroundColor`
- If `AppTheme.backgroundDark` is used in a non-Scaffold context (like a Container), still use `Theme.of(context).scaffoldBackgroundColor` — it resolves to the same value and keeps the semantic meaning
- Most files already have `context` available in their `build(BuildContext context)` methods
- For files that use the color in a variable/constant declaration OUTSIDE a build method (like in feed_providers.dart), investigate whether the reference can be moved to a build context or needs a different approach
- Make sure each file's `import` of AppTheme is kept if AppTheme is still used for non-Dark constants (like voltLime, textPrimary, etc.)
- Remove the AppTheme import ONLY if the file has NO remaining AppTheme references after replacement

After each directory group, verify with: `grep -r "AppTheme\.\w*Dark" mobile/lib/src/features/{directory}/ --include="*.dart"`

> verification: cd mobile && grep -r "AppTheme\.backgroundDark\|AppTheme\.cardDark\|AppTheme\.surfaceVariantDark\|AppTheme\.surfaceDark" lib/src/features/ --include="*.dart" -c
> verification: cd mobile && grep -r "AppTheme\.background[^D]\|AppTheme\.surfaceVariant[^D]" lib/src/features/ --include="*.dart" -c
  </action>
  <verify>
cd mobile && grep -r "AppTheme\.backgroundDark\|AppTheme\.cardDark\|AppTheme\.surfaceVariantDark\|AppTheme\.surfaceDark" lib/src/features/ --include="*.dart" -c
cd mobile && grep -r "AppTheme\.background[^D]\|AppTheme\.surfaceVariant[^D]" lib/src/features/ --include="*.dart" -c
  </verify>
  <done>All 253 AppTheme.*Dark references replaced with Theme.of(context) equivalents across all 47 files.</done>
</task>

<task type="auto">
  <name>Task 3: Remove deprecated constants and verify</name>
  <files>mobile/lib/src/core/theme/app_theme.dart</files>
  <action>
Final cleanup and verification.

**Step 1: Remove the deprecated static constants from app_theme.dart**

Delete these lines (or the deprecated versions) from the AppTheme class:

- `static const Color backgroundDark = Color(0xFF0D0F11);`
- `static const Color surfaceDark = Color(0xFF161B22);`
- `static const Color surfaceVariantDark = Color(0xFF21262D);`
- `static const Color cardDark = Color(0xFF1C2128);`
- `static const Color background = backgroundDark;` (alias)
- `static const Color surfaceVariant = surfaceVariantDark;` (alias)

**IMPORTANT**: Before deleting, verify that `darkTheme` doesn't reference these constants by name. If it does (e.g., `scaffoldBackgroundColor: backgroundDark`), inline the hex value: `scaffoldBackgroundColor: const Color(0xFF0D0F11)`.

**Step 2: Full codebase verification**

Run these commands to confirm zero remaining references:

```bash
cd mobile && grep -r "AppTheme\.\w*Dark" lib/ --include="*.dart"
cd mobile && grep -r "AppTheme\.background[^D]" lib/ --include="*.dart"
cd mobile && grep -r "AppTheme\.surfaceVariant[^D]" lib/ --include="*.dart"
```

All three should return empty (0 matches).

**Step 3: Run flutter analyze**

```bash
cd mobile && flutter analyze
```

There should be zero NEW errors introduced by these changes. Pre-existing warnings are acceptable.

> verification: cd mobile && grep -r "AppTheme\.\w*Dark" lib/ --include="*.dart" | wc -l
> verification: cd mobile && flutter analyze 2>&1 | tail -5
  </action>
  <verify>
cd mobile && grep -r "AppTheme\.\w*Dark" lib/ --include="*.dart" | wc -l
cd mobile && flutter analyze 2>&1 | tail -5
  </verify>
  <done>Zero AppTheme.*Dark references remain. flutter analyze passes. THEME-01 satisfied.</done>
</task>

</tasks>

<verification>
- [ ] `grep -r "AppTheme\.\w*Dark" mobile/lib/ --include="*.dart"` returns 0 matches
- [ ] `grep -r "AppTheme\.background[^D]" mobile/lib/ --include="*.dart"` returns 0 matches (alias removed)
- [ ] ColorScheme in darkTheme includes `surfaceContainerHigh` and `surfaceContainerHighest` with correct hex values
- [ ] Static backgroundDark, surfaceDark, surfaceVariantDark, cardDark constants removed from AppTheme
- [ ] `flutter analyze` passes with no new errors
- [ ] No production backend files were modified
</verification>

<success_criteria>

- Zero `AppTheme.*Dark` references remain in mobile code
- All replaced with `Theme.of(context)` theme-aware equivalents
- `flutter analyze` passes with no new errors
- Visual appearance is identical (same hex values mapped through ColorScheme)
</success_criteria>

<output>
After completion, create `.planning/phases/24-dark-color-cleanup/24-01-SUMMARY.md`

STATE updates expected after execution:

- Mark Phase 24 plan 01 as complete
- Update THEME-01 requirement status
- Phase 24 status: executed, pending review
</output>
