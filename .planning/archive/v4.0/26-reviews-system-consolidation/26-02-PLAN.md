---
phase: 26-reviews-system-consolidation
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - backend/src/services/ReviewService.ts
  - backend/src/controllers/ReviewController.ts
  - backend/src/routes/reviewRoutes.ts
  - backend/src/index.ts
  - backend/src/types/index.ts
  - backend/src/controllers/ClaimController.ts
  - backend/src/routes/claimRoutes.ts
  - backend/src/__tests__/services/ReviewService.test.ts
  - backend/src/__tests__/services/ReviewService.integration.test.ts
files_forbidden:
  - backend/migrations/**
  - mobile/**
sequential_files: []
expected_artifacts: []
autonomous: false
agents:
  - engineering-senior-developer
requirements:
  - DEBT-03
verification_commands:
  - "cd backend && npm test 2>&1 | grep -E 'Tests:|Test Suites:'"
  - "test ! -f backend/src/services/ReviewService.ts && echo 'deleted' || echo 'still exists'"
  - "test ! -f backend/src/controllers/ReviewController.ts && echo 'deleted' || echo 'still exists'"

must_haves:
  truths:
    - "ReviewService.ts is deleted from the codebase"
    - "ReviewController.ts is deleted from the codebase"
    - "reviewRoutes.ts is deleted from the codebase"
    - "/api/reviews route registration removed from index.ts"
    - "ClaimController.respondToReview() method and its route removed"
    - "Review-related types removed from types/index.ts"
    - "ReviewService test files deleted"
    - "Full backend test suite passes with zero failures"
  artifacts: []
  key_links:
    - from: "backend/src/index.ts"
      to: "backend/src/routes/reviewRoutes.ts"
      via: "Remove app.use('/api/reviews', reviewRoutes) and import"
      pattern: "reviewRoutes"
---

<objective>
Delete the entire review stack from the backend: ReviewService, ReviewController, review routes, review types, ClaimController.respondToReview(), and all review test files.

Purpose: With DEBT-02 satisfied (ratings computed from checkins), the review system is fully orphaned and can be safely deleted.
Output: No ReviewService, ReviewController, or /api/reviews endpoint exists. `npm test` passes.
</objective>

<context>
@.planning/phases/26-reviews-system-consolidation/26-CONTEXT.md
@.planning/phases/26-reviews-system-consolidation/26-01-SUMMARY.md

Key files to delete:
@backend/src/services/ReviewService.ts
@backend/src/controllers/ReviewController.ts
@backend/src/routes/reviewRoutes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ReviewService, ReviewController, and routes</name>
  <files>backend/src/services/ReviewService.ts, backend/src/controllers/ReviewController.ts, backend/src/routes/reviewRoutes.ts, backend/src/index.ts</files>
  <action>
1. Delete these files entirely:
   - `backend/src/services/ReviewService.ts`
   - `backend/src/controllers/ReviewController.ts`
   - `backend/src/routes/reviewRoutes.ts`

2. In `backend/src/index.ts`:
   - Remove the import: `import reviewRoutes from './routes/reviewRoutes';` (or similar)
   - Remove the route registration: `app.use('/api/reviews', reviewRoutes);`

3. Verify no other files import ReviewService or ReviewController:
   ```
   grep -rn "ReviewService\|ReviewController\|reviewRoutes" backend/src/ --include="*.ts" | grep -v __tests__ | grep -v "\.d\.ts"
   ```
   Fix any remaining imports.

> verification: test ! -f backend/src/services/ReviewService.ts && echo "deleted" || echo "still exists"
> verification: test ! -f backend/src/controllers/ReviewController.ts && echo "deleted" || echo "still exists"
  </action>
  <verify>
test ! -f backend/src/services/ReviewService.ts && echo "deleted" || echo "still exists"
test ! -f backend/src/controllers/ReviewController.ts && echo "deleted" || echo "still exists"
  </verify>
  <done>ReviewService, ReviewController, and reviewRoutes deleted. Route removed from index.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Remove owner response and review types</name>
  <files>backend/src/controllers/ClaimController.ts, backend/src/routes/claimRoutes.ts, backend/src/types/index.ts</files>
  <action>
1. In `backend/src/controllers/ClaimController.ts`:
   - Remove the `respondToReview()` method entirely
   - Remove any import of ReviewService
   - Keep all other ClaimController methods (they handle venue/band claims, not reviews)

2. In `backend/src/routes/claimRoutes.ts`:
   - Remove the route for `POST /api/claims/reviews/:reviewId/respond`
   - Keep all other claim routes

3. In `backend/src/types/index.ts`:
   - Remove the `Review` interface
   - Remove the `CreateReviewRequest` interface
   - Remove the `ReviewHelpfulness` interface
   - Remove the `OwnerReviewResponse` interface
   - Keep all non-review types

> verification: cd backend && grep -c "respondToReview\|ReviewService" src/controllers/ClaimController.ts || echo "0"
> verification: cd backend && grep -c "Review " src/types/index.ts || echo "0"
  </action>
  <verify>
cd backend && grep -c "respondToReview\|ReviewService" src/controllers/ClaimController.ts || echo "0"
cd backend && grep -c "interface Review " src/types/index.ts || echo "0"
  </verify>
  <done>Owner response endpoint removed. Review types cleaned from types/index.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Delete test files and verify</name>
  <files>backend/src/__tests__/services/ReviewService.test.ts, backend/src/__tests__/services/ReviewService.integration.test.ts</files>
  <action>
1. Delete these test files:
   - `backend/src/__tests__/services/ReviewService.test.ts`
   - `backend/src/__tests__/services/ReviewService.integration.test.ts`

2. Run the full test suite:
   ```
   cd backend && npm test
   ```
   Verify zero failures. If any tests fail because they import ReviewService, fix those imports.

> verification: cd backend && npm test 2>&1 | grep -E "Tests:|Test Suites:"
> verification: test ! -f backend/src/__tests__/services/ReviewService.test.ts && echo "deleted"
  </action>
  <verify>
cd backend && npm test 2>&1 | grep -E "Tests:|Test Suites:"
  </verify>
  <done>Review test files deleted. Full test suite passes. DEBT-03 satisfied.</done>
</task>

</tasks>

<verification>
- [ ] ReviewService.ts deleted
- [ ] ReviewController.ts deleted
- [ ] reviewRoutes.ts deleted
- [ ] `/api/reviews` route removed from index.ts
- [ ] ClaimController.respondToReview() removed
- [ ] Review types removed from types/index.ts
- [ ] Review test files deleted
- [ ] `npm test` passes with zero failures
</verification>

<success_criteria>
- ReviewService, ReviewController, `/api/reviews` endpoint deleted
- Owner response endpoint removed from ClaimController
- Review types cleaned from type definitions
- Backend test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/26-reviews-system-consolidation/26-02-SUMMARY.md`
</output>
