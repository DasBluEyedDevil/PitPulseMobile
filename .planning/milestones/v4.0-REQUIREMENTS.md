# Requirements: v4.0 — Technical Consolidation & Launch Hardening

## Launch Ops

- [x] **OPS-01**: All exposed secrets (DB password, JWT_SECRET, SetlistFM key) rotated in Railway production environment
- [ ] **OPS-02**: NODE_ENV=production set, all third-party env vars configured in Railway. 10/13 done 2026-04-19 (Sentry ✓, Firebase ✓, RevenueCat ✓, R2+Cloudflare ✓ ×5, Redis ✓, Ticketmaster ✓). Remaining: Resend (deferred), Google OAuth, Apple Developer.
- [x] **OPS-03**: Migration 039 applied to production DB and demo seed data populated

## Test Health

- [x] **TEST-01**: All pre-existing test failures fixed — full test suite passes with zero failures

## Theme Cleanup

- [x] **THEME-01**: Zero `AppTheme.*Dark` references remain in mobile code; all replaced with theme-aware equivalents

## Technical Debt

- [x] **DEBT-01**: CheckinCreatorService contains only event-first flow — legacy `createCheckin(bandId + venueId)` path and dual-write logic removed
- [x] **DEBT-02**: Venue and band aggregate ratings computed from `checkin_band_ratings` + `checkins.venue_rating` (not from `reviews` table)
- [x] **DEBT-03**: ReviewService, ReviewController, `/api/reviews` endpoint, and all review test files deleted from codebase
- [x] **DEBT-04**: `reviews` and `review_helpfulness` database tables dropped via migration

## Validation

- [x] **VAL-01**: Full test suite, smoke tests, aggregate verification, and `flutter analyze` all pass after all changes

---

**Total: 9 requirements across 5 categories**
**Phases: 22-27**
