# Plan: Resolve Errors and Warnings

## Summary

The errors.txt contains 2 legitimate Dart code warnings (unused imports) and thousands of warnings from generated HTML coverage reports that should be excluded from analysis.

## Issues to Fix

### 1. Mobile: Remove unused import in venue_repository.dart

**File:** `mobile/lib/src/features/venues/data/venue_repository.dart:5`
**Issue:** Unused import `../../../core/error/failures.dart`
**Action:** Remove the import statement on line 5

### 2. Mobile: Remove unused import in subscription_providers.dart

**File:** `mobile/lib/src/features/subscription/presentation/subscription_providers.dart:4`
**Issue:** Unused import `../../../core/api/dio_client.dart`
**Action:** Remove the import statement on line 4

### 3. Configure analysis_options.yaml to exclude coverage directory

**File:** `mobile/analysis_options.yaml`
**Issue:** Backend coverage HTML files are being analyzed, generating thousands of obsolete attribute warnings
**Action:** Add `backend/coverage/` to the `exclude:` section (or create if doesn't exist)

## Implementation Steps

1. Read `mobile/lib/src/features/venues/data/venue_repository.dart` to verify line 5 contains the unused import
2. Edit to remove line 5 (import of `../../../core/error/failures.dart`)
3. Read `mobile/lib/src/features/subscription/presentation/subscription_providers.dart` to verify line 4 contains the unused import
4. Edit to remove line 4 (import of `../../../core/api/dio_client.dart`)
5. Read `mobile/analysis_options.yaml` to check current configuration
6. Add `backend/coverage/` to exclude patterns
7. Run `flutter analyze` to verify all warnings are resolved

## Verification Commands

```bash
cd mobile
flutter analyze
```

Expected result: No errors or warnings (excluding any existing in dependencies)
